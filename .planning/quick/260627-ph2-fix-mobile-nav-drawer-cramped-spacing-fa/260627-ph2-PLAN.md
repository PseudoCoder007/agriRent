---
phase: quick-260627-ph2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(farmer)/layout.tsx
  - src/app/(owner)/layout.tsx
  - src/app/(auth)/login/page.tsx
  - src/app/(auth)/signup/page.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "Opening the mobile nav drawer (Sheet) on farmer or owner layouts shows spaciously padded nav items with a visible hover/active background, not bare cramped text links"
    - "Email, password, and full-name input fields on the login and signup pages have a clearly visible border against the white auth card, matching the existing GoogleOAuthButton's border-slate-300 for visual consistency"
  artifacts:
    - path: "src/app/(farmer)/layout.tsx"
      provides: "Farmer mobile nav drawer with padded, hover-styled SheetClose nav items (4 links)"
    - path: "src/app/(owner)/layout.tsx"
      provides: "Owner mobile nav drawer with padded, hover-styled SheetClose nav items (2 links)"
    - path: "src/app/(auth)/login/page.tsx"
      provides: "Login Input/PasswordInput usages with visible border-slate-300 className override"
    - path: "src/app/(auth)/signup/page.tsx"
      provides: "Signup Input/PasswordInput usages with visible border-slate-300 className override"
  key_links:
    - from: "src/app/(farmer)/layout.tsx"
      to: "src/components/ui/sheet.tsx (SheetClose render prop)"
      via: "className override on the rendered Link, not a change to Sheet primitives"
      pattern: "rounded-md.*px-3.*py-2\\.5"
    - from: "src/app/(auth)/login/page.tsx"
      to: "src/components/ui/input.tsx"
      via: "className passed through cn() at the call site, base input.tsx untouched"
      pattern: "border-slate-300"
---

<objective>
Fix two scoped, user-reported UI bugs: (1) the mobile nav Sheet drawer in both farmer and owner layouts renders cramped, padding-less nav links with no hover feedback, and (2) the login/signup page Input/PasswordInput fields have a border so faint against the white card that they're nearly invisible.

Purpose: Both are direct user-reported visual defects blocking comfortable use on mobile and on the auth pages. Pure CSS/className fixes — no new components, no logic changes, no schema/migration/server changes, no changes to shared base components (`input.tsx`, `password-input.tsx`, `globals.css`).

Output: Updated farmer/owner layout drawers with spacious, hover-styled nav items; updated login/signup pages with visibly bordered inputs.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@src/app/(farmer)/layout.tsx
@src/app/(owner)/layout.tsx
@src/app/(auth)/login/page.tsx
@src/app/(auth)/signup/page.tsx
@src/components/ui/input.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle mobile nav drawer items in farmer and owner layouts</name>
  <files>src/app/(farmer)/layout.tsx, src/app/(owner)/layout.tsx</files>
  <action>
In `src/app/(farmer)/layout.tsx`, update the `<nav className="flex flex-col gap-1 p-4">` block inside `SheetContent` (currently containing 4 `SheetClose` items: Dashboard, Browse, Favorites, Chat). For each `SheetClose`'s `render={<Link ... className="..." />}`, replace the className `"text-muted-foreground hover:text-foreground"` with `"flex items-center rounded-md px-3 py-2.5 text-sm font-medium text-muted-foreground transition-colors hover:bg-accent hover:text-accent-foreground"`. Keep every other prop (`nativeButton={false}`, `href`, the `SheetClose` wrapping, the link text children) exactly as-is — only the className value on the inner `Link` changes. Drop the nav container's `gap-1` to no gap (or keep `gap-1`; use judgment) since the new `py-2.5` padding already creates comfortable spacing between items — the end visual result must look spacious, not cramped, with no double-spacing from both gap and padding stacking awkwardly. Do not touch the desktop `<nav className="hidden gap-3 text-sm sm:flex">` block or its Link classNames — this fix is scoped to the mobile Sheet drawer only.

Apply the identical pattern in `src/app/(owner)/layout.tsx` to its `<nav className="flex flex-col gap-1 p-4">` block inside `SheetContent` (2 `SheetClose` items: Dashboard, Chat) — same className replacement on each inner `Link`, same gap adjustment, same scoping (desktop nav untouched).
  </action>
  <verify>
    <automated>cd "C:\Users\alisa\OneDrive\Desktop\AgriRent" && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>All 4 farmer mobile nav links and both owner mobile nav links render with rounded-md, px-3 py-2.5 padding, and hover:bg-accent hover:text-accent-foreground in their className; desktop nav classNames in both layouts are unchanged; tsc passes with no type errors.</done>
</task>

<task type="auto">
  <name>Task 2: Add visible border override to login/signup Input and PasswordInput fields</name>
  <files>src/app/(auth)/login/page.tsx, src/app/(auth)/signup/page.tsx</files>
  <action>
In `src/app/(auth)/login/page.tsx`, add `className="border-slate-300"` to the email `<Input type="email" placeholder="you@example.com" {...field} />` and to the `<PasswordInput {...field} />` for the password field. Both components forward `className` through `cn()` (confirmed: `Input` merges via `cn(baseClasses, className)`; `PasswordInput` merges via `cn("pr-10", className)`, and tailwind-merge resolves the conflicting `border-*` utility by keeping the last one in the merged class list, so the override applies cleanly without fighting the base `border-input` token). Do not modify `src/components/ui/input.tsx` or `src/components/ui/password-input.tsx`.

In `src/app/(auth)/signup/page.tsx`, add the identical `className="border-slate-300"` to all three field usages: the full-name `<Input placeholder="Jane Farmer" {...field} />`, the email `<Input type="email" placeholder="you@example.com" {...field} />`, and the `<PasswordInput {...field} />`. Do not add it to the `RadioGroupItem`/`Label` role selector — those are unrelated to this bug. This matches the existing `border-slate-300` already used on both pages' `GoogleOAuthButton` for visual consistency.
  </action>
  <verify>
    <automated>cd "C:\Users\alisa\OneDrive\Desktop\AgriRent" && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>Every Input/PasswordInput usage on login/page.tsx (2 fields) and signup/page.tsx (3 fields) has className="border-slate-300" added at the call site; src/components/ui/input.tsx and password-input.tsx remain unmodified; tsc passes with no type errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None | This plan is a pure presentational/CSS change — no new user input handling, no new trust boundary, no data flow change. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-ph2-01 | None | N/A | accept | No new attack surface — className-only changes to existing, already-reviewed nav links and form inputs; no logic, validation, or data-handling code is touched. |
</threat_model>

<verification>
1. `npx tsc --noEmit` passes for both tasks (no type errors introduced).
2. Manual visual check: open the app at a mobile viewport (<640px), tap the hamburger menu on `/farmer/dashboard` and `/owner/dashboard` — drawer nav items show clear padding, rounded hover background on tap/hover, comfortable spacing (not cramped).
3. Manual visual check: visit `/login` and `/signup` — every input field border is clearly visible against the white card (matching the visible border style already on the Google button).
</verification>

<success_criteria>
- Farmer and owner mobile Sheet drawer nav items have visible padding, rounded hover background, and comfortable spacing.
- Login and signup Input/PasswordInput fields have a clearly visible border (border-slate-300), scoped only to those two pages.
- `src/components/ui/input.tsx`, `src/components/ui/password-input.tsx`, and `globals.css` remain unmodified.
- `npx tsc --noEmit` passes.
</success_criteria>

<output>
Create `.planning/quick/260627-ph2-fix-mobile-nav-drawer-cramped-spacing-fa/260627-ph2-SUMMARY.md` when done
</output>
