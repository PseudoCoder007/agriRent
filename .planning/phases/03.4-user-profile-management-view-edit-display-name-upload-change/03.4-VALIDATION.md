---
phase: 03.4
slug: user-profile-management-view-edit-display-name-upload-change
status: draft
nyquist_compliant: true
wave_0_complete: false
created: 2026-06-28
---

# Phase 03.4 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Vitest `^4.1.9` [VERIFIED: package.json] |
| **Config file** | `vitest.config.ts` (project root) — `environment: "node"`, includes `src/**/*.test.ts` |
| **Quick run command** | `npx vitest run src/lib/validations/profile.schema.test.ts` |
| **Full suite command** | `npx vitest run` |
| **Estimated runtime** | ~5 seconds |

---

## Sampling Rate

- **After every task commit:** Run `npx vitest run src/lib/validations/profile.schema.test.ts`
- **After every plan wave:** Run `npx vitest run` (full suite)
- **Before `/gsd-verify-work`:** Full suite must be green, plus manual end-to-end checks below
- **Max feedback latency:** ~5 seconds (no e2e framework — manual browser verification closes the remaining gap)

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 03.4-01-T1 | 01 | 1 | PROFILE-01/02/03 | T-03.4-01 | Migration pushes `phone`/`avatar_url`/`avatar_updated_at` to live `public.users`; existing `users update own row` RLS policy covers writes; phone-PII exposure via existing public-read policy explicitly documented in SQL comment, not silently inherited | manual (schema push) | `supabase db push` then `supabase gen types typescript --linked > types/database.ts` | N/A — infra step | ⬜ pending |
| 03.4-01-T2 | 01 | 1 | PROFILE-01, PROFILE-02, PROFILE-03 | — | `updateProfileSchema` validates display name (non-empty) and phone (optional, regex-matched); `avatarFileSchema` validates MIME type + 5MB size limit | unit | `npx vitest run src/lib/validations/profile.schema.test.ts` | ❌ W0 — does not exist yet | ⬜ pending |
| 03.4-02-T1..T3 | 02 | 2 | PROFILE-01, PROFILE-03 | T-03.4-01 (phone exposure tradeoff applies here at the render layer) | `profile.service.ts`/`profile.actions.ts` re-derive `userId` server-side, never trust client payload; display name + phone changes persist and render immediately via `AccountMenu`/profile pages | unit + manual | `npx vitest run` (service/action unit coverage if present) + manual browser verification | ❌ W0 for any new unit tests; manual required regardless | ⬜ pending |
| 03.4-03-T1..T2 | 03 | 3 | PROFILE-02 | Tampering / Elevation of Privilege (cross-user avatar overwrite); Tampering / stored-XSS (malicious MIME upload) | Avatar upload path is fixed to `${userId}/avatar.<ext>` with `upsert: true`; Storage bucket RLS restricts writes to `(storage.foldername(name))[1] = auth.uid()::text`; cache-busted URL (`avatar_updated_at` query param) renders everywhere the avatar is shown | manual (no Storage emulator in this project's test setup) | Manual browser verification: upload, replace, refresh, confirm new image renders without stale CDN cache | N/A — no Storage test harness exists | ⬜ pending |
| ALL | 01/02/03 | — | PROFILE-01/02/03 | — | Display name/photo/phone change persists across a session reload | manual | Manual browser verification: edit profile, refresh page, confirm values persist | N/A — no e2e framework (Playwright/Cypress) in this codebase | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `src/lib/validations/profile.schema.test.ts` — covers PROFILE-01, PROFILE-02, PROFILE-03 (unit tests for `updateProfileSchema`/`avatarFileSchema`, mirroring the structure of the existing `equipment.schema.test.ts`)
- [ ] No new shared fixtures or framework install needed — `vitest.config.ts` already includes any `src/**/*.test.ts` file automatically

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Display name edit persists and renders in nav for both roles | PROFILE-01 | No e2e framework in this codebase | Log in as farmer, open AccountMenu → Edit profile, change name, save, confirm nav shows new name immediately and after refresh; repeat as owner |
| Avatar upload/replace renders correctly without stale CDN cache | PROFILE-02 | No Storage test harness; CDN caching behavior only observable live | Upload an avatar, confirm it renders; replace it, confirm the new image appears within the page (not the stale cached one) without requiring a hard refresh |
| Phone number add/edit persists across session | PROFILE-03 | No e2e framework in this codebase | Edit profile, add/change phone number, save, log out and back in, confirm phone value persisted |
| Oversized/wrong-MIME avatar upload is rejected with a clear error | PROFILE-02 | Requires browser file-picker interaction | Attempt to upload a >5MB file and a non-image file (e.g. `.txt` renamed to `.jpg`); confirm both are rejected with the error copy defined in UI-SPEC, not a silent failure |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [x] Sampling continuity: no 3 consecutive tasks without automated verify (schema/Zod tasks are automated; UI/avatar tasks are manual by necessity — no Storage/e2e harness exists in this codebase, consistent with how Phases 1-3 were verified)
- [ ] Wave 0 covers all MISSING references
- [x] No watch-mode flags
- [x] Feedback latency < 5s (unit layer); manual layer is the accepted ceiling for UI/Storage behavior in this codebase
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
