---
phase: 03.1
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - "src/app/(farmer)/layout.tsx"
  - "src/app/(owner)/layout.tsx"
autonomous: true
requirements: [NAV-01]

must_haves:
  truths:
    - "On desktop (sm: and above), the farmer and owner headers are replaced by a vertical sidebar with three zones: top utility row (ThemeToggle + NotificationBell together), middle nav links stacked vertically, bottom logout pinned via mt-auto"
    - "On mobile, the existing Sheet drawer is restructured into the same 3-zone vertical layout: top utility row, flex-1 nav block, bottom logout — without losing the existing SheetClose/nativeButton={false} pattern on nav Links"
    - "Nav link rows use px-3 py-2 (not py-2.5) in both desktop sidebar and mobile Sheet drawer"
    - "The structural pattern is identical between farmer and owner layouts, differing only in link count/labels (farmer: Dashboard, Browse, Favorites, Chat; owner: Dashboard, Chat)"
  artifacts:
    - path: "src/app/(farmer)/layout.tsx"
      provides: "3-zone vertical sidebar (desktop) + restructured 3-zone Sheet drawer (mobile)"
    - path: "src/app/(owner)/layout.tsx"
      provides: "3-zone vertical sidebar (desktop) + restructured 3-zone Sheet drawer (mobile), identical structure to farmer layout with 2 nav links instead of 4"
  key_links:
    - from: "src/app/(farmer)/layout.tsx"
      to: "src/components/auth/logout-button.tsx"
      via: "<LogoutButton /> rendered inside a div with mt-auto"
      pattern: "mt-auto"
    - from: "src/app/(farmer)/layout.tsx"
      to: "src/components/ui/sheet.tsx"
      via: "SheetClose nativeButton={false} render prop wrapping each nav Link"
      pattern: "SheetClose"
---

<objective>
Restructure the farmer and owner role-scoped navigation from a horizontal header-bar pattern into a vertical sidebar-style 3-zone layout, applied identically to both desktop (persistent sidebar) and mobile (Sheet drawer): a top utility row pairing `ThemeToggle` + `NotificationBell`, vertically stacked nav links in the middle with corrected `px-3 py-2` spacing, and `LogoutButton` pinned to the bottom via `mt-auto`.

Purpose: Per NAV-01, the current flat horizontal nav row must become a clear sidebar layout — theme toggle and notification bell sharing a top utility row, nav links occupying the main middle area with proper spacing, logout pinned at the bottom — consistently for both farmer and owner roles, on both desktop and mobile.

Output: `src/app/(farmer)/layout.tsx` and `src/app/(owner)/layout.tsx` rewritten with a `flex` row layout (sidebar + main content) on desktop, and a restructured 3-zone `SheetContent` on mobile, both built from the existing `ThemeToggle`, `NotificationBell`, `LogoutButton`, `Sheet` components without modifying any of those components' own source files.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03.1-auth-navigation-ui-fixes-inserted/03.1-UI-SPEC.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure farmer layout into 3-zone sidebar (desktop) + 3-zone Sheet (mobile)</name>
  <files>src/app/(farmer)/layout.tsx</files>
  <read_first>
    Read src/app/(farmer)/layout.tsx in full (148 lines). Current structure: a single `<header className="flex items-center justify-between border-b p-4">` containing (a) brand span + horizontal `<nav className="hidden gap-3 text-sm sm:flex">` with 4 Links (Dashboard, Browse, Favorites, Chat) using `text-muted-foreground hover:text-foreground`, (b) a `hidden sm:flex` utility div with ThemeToggle/NotificationBell/LogoutButton inline, (c) a `Sheet` for mobile with a `SheetTrigger` Menu button and `SheetContent side="right" className="w-64"` containing a `<nav className="flex flex-col p-4">` of `SheetClose`-wrapped Links each at `px-3 py-2.5` plus a bottom `<div className="flex flex-col gap-2 border-t p-4 pt-3">` with ThemeToggle/NotificationBell/LogoutButton stacked. The 4 `SheetClose` blocks use the `nativeButton={false}` render-prop pattern — this exact pattern (established in Phase 02.1-06 to fix a base-ui Dialog Close primitive bug) MUST be preserved unchanged on every nav Link inside the Sheet.
  </read_first>
  <action>
    Replace the file's return statement. Wrap the whole layout in `<div className="flex min-h-screen">`. Build a desktop-only `<aside className="hidden w-64 flex-col border-r sm:flex">` (use `flex flex-col` so its children stack vertically) containing three children in order: (1) a top utility row `<div className="flex items-center justify-between gap-2 border-b p-4">` holding the brand span `<span className="text-sm font-medium">AgriRent — Farmer</span>` on its own line above, followed by a second row inside the same div or a sibling div with `ThemeToggle` and `NotificationBell` side by side (`<div className="flex items-center gap-2"><ThemeToggle /><NotificationBell userId={userData.user.id} /></div>`) — keep the brand label visible per spec; (2) a nav block `<nav className="flex flex-1 flex-col gap-1 p-4">` containing 4 `Link` elements (Dashboard -> /farmer/dashboard, Browse -> /browse, Favorites -> /farmer/favorites, Chat -> /farmer/chat) each with className `"rounded-md px-3 py-2 text-sm font-semibold text-muted-foreground transition-colors hover:bg-accent hover:text-accent-foreground"` (changed from the old horizontal nav's `text-muted-foreground hover:text-foreground` with no padding, to the spec's `px-3 py-2` + `font-semibold` sidebar link style); (3) a bottom zone `<div className="mt-auto border-t p-4"><LogoutButton /></div>`. Outside the `<aside>`, keep the existing mobile `Sheet`/`SheetTrigger` (the hamburger trigger button), but move its trigger to render only below `sm:` (it already is `sm:hidden`-gated via the Button's `className="h-11 w-11 sm:hidden"` — keep that). Inside `SheetContent`, restructure to 3 zones: replace the sr-only-only `SheetTitle` setup with `<SheetContent side="right" className="flex w-64 flex-col"><SheetTitle className="sr-only">Navigation menu</SheetTitle><div className="flex items-center justify-between gap-2 border-b p-4"><ThemeToggle /><NotificationBell userId={userData.user.id} /></div><nav className="flex flex-1 flex-col gap-1 p-4">` then the same 4 `SheetClose` blocks (unchanged `nativeButton={false}` render-prop pattern, only changing each Link's className from `px-3 py-2.5` to `px-3 py-2`) `</nav><div className="mt-auto border-t p-4"><LogoutButton /></div></SheetContent>`. Finally, wrap `{children}` in `<main className="flex-1 sm:p-0">{children}</main>` as a sibling of `<aside>` inside the outer flex div, and place the mobile `Sheet` trigger as a small fixed/inline element visible only below `sm:` (e.g. a top bar `<div className="flex items-center justify-between border-b p-4 sm:hidden"><span className="text-sm font-medium">AgriRent — Farmer</span><Sheet>...</Sheet></div>` placed above `<main>` content on mobile, since the desktop `<aside>` is hidden below `sm:` and mobile needs its own way to open the Sheet trigger).
  </action>
  <acceptance_criteria>
    - File contains `<aside` with `hidden w-64 flex-col border-r sm:flex` (or equivalent class set achieving: hidden on mobile, flex column, fixed width, right border, visible at sm: and above)
    - The aside's nav `<Link>` elements use `px-3 py-2` (not `py-2.5`) and `font-semibold`
    - A `<div` with `mt-auto` wraps `<LogoutButton />` inside the aside
    - `ThemeToggle` and `NotificationBell` appear together in a single flex row in both the desktop aside's top zone and the mobile Sheet's top zone
    - All 4 `SheetClose` blocks retain `nativeButton={false}` and their Link children, with className updated to `px-3 py-2`
    - The mobile Sheet's bottom zone contains only `LogoutButton` (ThemeToggle/NotificationBell removed from there, now living in the top zone)
    - A mobile-visible trigger (hamburger button opening the Sheet) still exists and is reachable below `sm:`
  </acceptance_criteria>
  <verify>
    <automated>grep -c "py-2.5" "src/app/(farmer)/layout.tsx"</automated>
  </verify>
  <done>Farmer layout renders a 3-zone desktop sidebar and a 3-zone mobile Sheet drawer; zero `py-2.5` occurrences remain; SheetClose/nativeButton pattern preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Apply identical 3-zone restructure to owner layout</name>
  <files>src/app/(owner)/layout.tsx</files>
  <read_first>
    Read src/app/(owner)/layout.tsx in full (120 lines) and the completed src/app/(farmer)/layout.tsx from Task 1 of this same plan (read it after writing it, in the same session). The owner layout is structurally identical to the pre-Task-1 farmer layout except it has only 2 nav links (Dashboard -> /owner/dashboard, Chat -> /owner/chat) instead of 4, and the brand span reads "AgriRent — Owner".
  </read_first>
  <action>
    Apply the exact same 3-zone restructure pattern from Task 1 to `src/app/(owner)/layout.tsx`: outer `<div className="flex min-h-screen">`, desktop `<aside className="hidden w-64 flex-col border-r sm:flex">` with (1) top utility row (brand span "AgriRent — Owner" + `ThemeToggle`/`NotificationBell` pair), (2) `<nav className="flex flex-1 flex-col gap-1 p-4">` with exactly 2 Links (Dashboard, Chat) at `rounded-md px-3 py-2 text-sm font-semibold text-muted-foreground transition-colors hover:bg-accent hover:text-accent-foreground`, (3) `<div className="mt-auto border-t p-4"><LogoutButton /></div>`. Restructure the mobile `SheetContent` identically: top utility row with `ThemeToggle`+`NotificationBell`, `<nav className="flex flex-1 flex-col gap-1 p-4">` with the 2 `SheetClose`-wrapped Links (unchanged `nativeButton={false}` pattern, className updated to `px-3 py-2`), bottom `<div className="mt-auto border-t p-4"><LogoutButton /></div>`. Add a mobile-only top bar with brand span + Sheet trigger above `<main>`, matching Task 1's pattern. Wrap `{children}` in `<main className="flex-1">{children}</main>` as a sibling of `<aside>`.
  </action>
  <acceptance_criteria>
    - File contains `<aside` with the same hidden/flex/width/border class set as the farmer layout
    - The aside's 2 nav `<Link>` elements (Dashboard, Chat) use `px-3 py-2` and `font-semibold`
    - A `<div` with `mt-auto` wraps `<LogoutButton />` inside the aside
    - `ThemeToggle` and `NotificationBell` appear together in the top zone of both desktop aside and mobile Sheet
    - Both `SheetClose` blocks (Dashboard, Chat) retain `nativeButton={false}` with className updated to `px-3 py-2`
    - Owner layout's structure matches farmer layout's structure 1:1 except for the 2-vs-4 link count and "Owner" vs "Farmer" brand label
  </acceptance_criteria>
  <verify>
    <automated>grep -c "py-2.5" "src/app/(owner)/layout.tsx"</automated>
  </verify>
  <done>Owner layout renders the identical 3-zone structure as farmer layout, scoped to its 2 nav links; zero `py-2.5` occurrences remain.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server-side role re-confirmation (pre-existing, unmodified) | Both layouts re-verify the session and role from `public.users` server-side on every render (`supabase.auth.getUser()` + role lookup + redirect). This plan does not touch that authorization logic — only the JSX returned after the existing role checks pass. |
| None new | No new server action, API route, or database query is introduced. The restructure only changes how `ThemeToggle`, `NotificationBell`, `LogoutButton`, and nav `Link`s are arranged in JSX — all four remain the same components with the same props (`userId={userData.user.id}` for `NotificationBell` is unchanged). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03.1-05 | Tampering | Layout JSX restructure (farmer/owner) | accept | Purely a rearrangement of existing, already-authorized UI elements into a different visual layout (flex row -> flex column sidebar). No new data is fetched, no new write path is introduced, and the pre-existing server-side role redirect logic (`if (profile.role === "owner") redirect(...)`) is preserved verbatim — it executes before the restructured JSX is ever returned. |
| T-03.1-06 | Denial of Service | `SheetClose`/`nativeButton={false}` pattern preserved | accept | This plan explicitly preserves the Phase 02.1-06 fix for the base-ui Dialog Close primitive bug rather than touching it, avoiding regression of `mobile nav drawer close-on-navigate`. No new client-side state or effect is introduced beyond what `Sheet`'s existing open/close state already manages. |
| T-03.1-SC | Tampering | No new package installs in this plan | accept | This plan modifies only existing first-party layout files. No `npm install` / `pip install` / `cargo add` commands are run. Package Legitimacy Gate not applicable. |
</threat_model>

<verification>
- `grep -c "py-2.5"` on both `(farmer)/layout.tsx` and `(owner)/layout.tsx` returns 0.
- Both layouts contain a `<aside` element with sidebar classes (`hidden`, `flex-col`, `border-r`, `sm:flex`, fixed width).
- Both layouts' `LogoutButton` sits inside a `mt-auto`-classed wrapper, both in the desktop aside and the mobile Sheet.
- `ThemeToggle` and `NotificationBell` appear adjacent (same parent flex row) in both the desktop top zone and mobile Sheet top zone, in both files.
- All `SheetClose` elements retain `nativeButton={false}`.
- Manual/human-deferred check (covered by Phase 3.1's overall human-verify step): resize browser below 640px and confirm the Sheet drawer opens with the 3-zone layout; resize above 640px and confirm the persistent sidebar renders with all 3 zones; navigate between farmer/owner dashboard, browse, favorites, chat and confirm nav links still route correctly.
</verification>

<success_criteria>
- Farmer and owner layouts both render a 3-zone vertical sidebar on desktop (`sm:` and above) and a 3-zone Sheet drawer on mobile.
- Nav link spacing is `px-3 py-2` in both layouts, both desktop and mobile, with zero remaining `py-2.5`.
- Logout is bottom-pinned via `mt-auto` in both layouts' desktop sidebar and mobile Sheet.
- The existing `SheetClose`/`nativeButton={false}` close-on-navigate fix from Phase 02.1-06 is unregressed.
- `npm run build` (or `next build`) completes without TypeScript/lint errors introduced by these changes.
</success_criteria>

<output>
Create `.planning/phases/03.1-auth-navigation-ui-fixes-inserted/03.1-03-SUMMARY.md` when done
</output>

## Artifacts this phase produces

- Desktop sidebar `<aside>` element (new structural pattern, no new component file) in both `src/app/(farmer)/layout.tsx` and `src/app/(owner)/layout.tsx` — 3-zone vertical flex column: top utility row, flex-1 nav links, mt-auto logout.
- Restructured `SheetContent` internal layout (same `Sheet`/`SheetContent`/`SheetClose`/`SheetTrigger`/`SheetTitle` components, new internal arrangement) in both layout files, mirroring the desktop 3-zone pattern.
- Corrected `px-3 py-2` nav-link spacing (replacing `py-2.5`) applied consistently across desktop and mobile nav links in both layouts.
