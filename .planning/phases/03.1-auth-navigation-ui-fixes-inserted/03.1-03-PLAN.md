---
phase: 03.1
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - "src/app/(farmer)/layout.tsx"
  - "src/app/(owner)/layout.tsx"
autonomous: true
requirements: [NAV-01]

must_haves:
  truths:
    - "On desktop (sm: and above), ThemeToggle + NotificationBell are visually grouped together as a distinct utility cluster, separate from LogoutButton"
    - "On desktop, LogoutButton is visually separated from the theme/notification cluster (its own grouping, not inline with them)"
    - "On mobile (Sheet drawer), ThemeToggle + NotificationBell appear together in a distinct row separate from LogoutButton, with LogoutButton pinned to the bottom via mt-auto"
    - "The existing branded header (AgriMateAIIcon + AgriRent wordmark + role subtitle), RoleNavLinks/RoleMobileNavLinks active-link highlighting, and SheetClose/nativeButton={false} pattern are preserved unchanged"
    - "The structural pattern is identical between farmer and owner layouts, differing only in link count/labels (farmer: Dashboard, Browse, Favorites, Chat; owner: Dashboard, Chat)"
  artifacts:
    - path: "src/app/(farmer)/layout.tsx"
      provides: "Desktop: ThemeToggle+NotificationBell utility cluster separated from bottom-pinned LogoutButton. Mobile Sheet: top utility row (ThemeToggle+NotificationBell) separated from bottom mt-auto LogoutButton row."
    - path: "src/app/(owner)/layout.tsx"
      provides: "Identical structure to farmer layout, scoped to owner's 2 nav links."
  key_links:
    - from: "src/app/(farmer)/layout.tsx"
      to: "src/components/auth/logout-button.tsx"
      via: "<LogoutButton /> rendered inside its own div, separate from the ThemeToggle/NotificationBell cluster"
      pattern: "LogoutButton"
    - from: "src/app/(farmer)/layout.tsx"
      to: "src/components/ui/sheet.tsx"
      via: "SheetClose nativeButton={false} render prop wrapping each nav Link (already present via RoleMobileNavLinks, unmodified)"
      pattern: "SheetClose"
---

<objective>
Finish the NAV-01 restructure on top of nav/brand work that was built in parallel (and is being kept, not discarded): `src/app/(farmer)/layout.tsx` and `src/app/(owner)/layout.tsx` already use a shared `RoleNavLinks`/`RoleMobileNavLinks` component pair (with active-route highlighting), a branded header (`AgriMateAIIcon` + "AgriRent" wordmark + role subtitle), and a working `SheetClose`/`nativeButton={false}` mobile drawer. What is still missing, per NAV-01's success criterion, is the **utility-row separation**: today `ThemeToggle`, `NotificationBell`, and `LogoutButton` are all grouped together in one block (desktop: a single `hidden items-center gap-2 sm:flex` div containing all three; mobile: a single bottom `mt-auto flex flex-col gap-3` div containing all three). This plan splits that single block into two distinct zones in both places: a `ThemeToggle` + `NotificationBell` utility cluster, and a separately-grouped, bottom-pinned `LogoutButton`.

Purpose: Per NAV-01, "theme toggle and notification bell share a top utility row" and "logout is pinned at the bottom" — these must be two visually distinct groups, not one flat list. This plan does NOT rebuild `RoleNavLinks`, `RoleMobileNavLinks`, the branded header, or the Sheet's open/close mechanics — those are existing, already-correct work from a parallel in-progress change and are explicitly out of scope to touch.

Output: `src/app/(farmer)/layout.tsx` and `src/app/(owner)/layout.tsx` with `ThemeToggle`+`NotificationBell` grouped as one cluster and `LogoutButton` grouped/pinned separately, in both the desktop header and the mobile Sheet drawer, applied identically to both files.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03.1-auth-navigation-ui-fixes-inserted/03.1-UI-SPEC.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Split farmer layout's utility block into a theme/bell cluster and a separated, bottom-pinned logout</name>
  <files>src/app/(farmer)/layout.tsx</files>
  <read_first>
    Read src/app/(farmer)/layout.tsx in its current state (122 lines). It already imports `AgriMateAIIcon` from `@/components/brand/agri-mate-ai-mark`, `RoleNavLinks` and `RoleMobileNavLinks` from `@/components/navigation/`, and renders a branded `<Link href="/farmer/dashboard">` header (logo + "AgriRent" + "Farmer workspace" subtitle) followed by `<RoleNavLinks items={farmerNavItems} className="hidden gap-4 text-sm font-medium sm:flex" />` inside the header's left-side div. Two places currently group `ThemeToggle`+`NotificationBell`+`LogoutButton` together: (1) desktop, the header's right-side `<div className="hidden items-center gap-2 sm:flex"><ThemeToggle /><NotificationBell userId={userData.user.id} /><LogoutButton /></div>`; (2) mobile, inside `SheetContent` after `<RoleMobileNavLinks items={farmerNavItems} />`, the `<div className="mt-auto flex flex-col gap-3 border-t border-border/70 px-4 py-4"><ThemeToggle /><NotificationBell userId={userData.user.id} /><LogoutButton /></div>`. Do not modify the branded `<Link>` header block, `RoleNavLinks`, `RoleMobileNavLinks`, `SheetHeader`, or any import — only restructure these two specific groupings.
  </read_first>
  <action>
    On the desktop header's right-side div, change `<div className="hidden items-center gap-2 sm:flex"><ThemeToggle /><NotificationBell userId={userData.user.id} /><LogoutButton /></div>` into two sibling elements inside a shared `<div className="hidden items-center gap-3 sm:flex">` wrapper: first `<div className="flex items-center gap-2 rounded-full border border-border/70 bg-muted/30 p-1"><ThemeToggle /><NotificationBell userId={userData.user.id} /></div>` (the utility cluster, visually grouped via its own border/background), then `<div className="border-l border-border/70 pl-3"><LogoutButton /></div>` (logout visually separated by a leading divider border). On mobile, change the Sheet's bottom block from one flat `<div className="mt-auto flex flex-col gap-3 border-t border-border/70 px-4 py-4">` containing all three components into two stacked rows: add a NEW top utility row directly inside `SheetContent` between `SheetHeader` and `<RoleMobileNavLinks items={farmerNavItems} />`: `<div className="flex items-center justify-between gap-2 border-b border-border/70 px-4 py-3"><ThemeToggle /><NotificationBell userId={userData.user.id} /></div>`, leave `<RoleMobileNavLinks items={farmerNavItems} />` exactly where it is and unmodified, and change the existing bottom block to contain only logout: `<div className="mt-auto border-t border-border/70 px-4 py-4"><LogoutButton /></div>`. Net effect on mobile: `SheetHeader` (brand) -> new top utility row (ThemeToggle+NotificationBell) -> `RoleMobileNavLinks` (nav links, middle) -> bottom logout row.
  </action>
  <acceptance_criteria>
    - Desktop header's right-side area contains `ThemeToggle` and `NotificationBell` inside one shared div, visually distinct (own border/background) from a second div containing only `LogoutButton`
    - Mobile `SheetContent` contains, in order: `SheetHeader` (unchanged), a new `<div` utility row with `ThemeToggle` and `NotificationBell` together, `<RoleMobileNavLinks items={farmerNavItems} />` (unchanged, untouched as a component), then a `mt-auto`-classed div containing only `<LogoutButton />`
    - `RoleNavLinks`, `RoleMobileNavLinks`, `AgriMateAIIcon`, `SheetHeader`, `SheetTitle`, `SheetDescription` usages and imports are unchanged
    - No `SheetClose`/`nativeButton={false}` pattern is touched (it lives inside `RoleMobileNavLinks`, which this task does not modify)
  </acceptance_criteria>
  <verify>
    <automated>grep -c "mt-auto" "src/app/(farmer)/layout.tsx"</automated>
  </verify>
  <done>Farmer layout's desktop header and mobile Sheet drawer each show ThemeToggle+NotificationBell as one visually distinct cluster and LogoutButton as a separately grouped, bottom-pinned element; branded header and nav components are unmodified.</done>
</task>

<task type="auto">
  <name>Task 2: Apply the identical utility-block split to owner layout</name>
  <files>src/app/(owner)/layout.tsx</files>
  <read_first>
    Read src/app/(owner)/layout.tsx in its current state (120 lines) and the completed src/app/(farmer)/layout.tsx from Task 1 of this same plan (read it after writing it, in the same session, to confirm the exact pattern to mirror). The owner layout is structurally identical to the farmer layout except it has only 2 nav items (`ownerNavItems`: Dashboard, Chat) and its branded header subtitle reads "Owner workspace" instead of "Farmer workspace" — both already correctly wired via `RoleNavLinks`/`RoleMobileNavLinks` with `ownerNavItems`.
  </read_first>
  <action>
    Apply the exact same restructure from Task 1 to `src/app/(owner)/layout.tsx`: desktop header's right-side div becomes a `<div className="hidden items-center gap-3 sm:flex">` wrapping a `<div className="flex items-center gap-2 rounded-full border border-border/70 bg-muted/30 p-1"><ThemeToggle /><NotificationBell userId={userData.user.id} /></div>` cluster and a `<div className="border-l border-border/70 pl-3"><LogoutButton /></div>` logout group. Mobile `SheetContent` gets a new `<div className="flex items-center justify-between gap-2 border-b border-border/70 px-4 py-3"><ThemeToggle /><NotificationBell userId={userData.user.id} /></div>` utility row inserted between `SheetHeader` and `<RoleMobileNavLinks items={ownerNavItems} />`, and the bottom block becomes `<div className="mt-auto border-t border-border/70 px-4 py-4"><LogoutButton /></div>` containing only logout. Do not touch `RoleNavLinks`, `RoleMobileNavLinks`, the branded header `<Link>` block, `SheetHeader`, or any import.
  </action>
  <acceptance_criteria>
    - Owner layout's desktop header right-side area matches farmer layout's structure: ThemeToggle+NotificationBell in one div, LogoutButton in a separate div
    - Owner layout's mobile `SheetContent` order matches farmer's: `SheetHeader`, new utility row (ThemeToggle+NotificationBell), `<RoleMobileNavLinks items={ownerNavItems} />`, bottom `mt-auto` logout-only div
    - `RoleNavLinks`, `RoleMobileNavLinks`, `AgriMateAIIcon`, `SheetHeader` usages and imports are unchanged
    - Owner layout's structure matches farmer layout's structure 1:1 except for the 2-vs-4 link count and "Owner" vs "Farmer" labels
  </acceptance_criteria>
  <verify>
    <automated>grep -c "mt-auto" "src/app/(owner)/layout.tsx"</automated>
  </verify>
  <done>Owner layout mirrors farmer layout's utility-cluster/logout split, scoped to its 2 nav links.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server-side role re-confirmation (pre-existing, unmodified) | Both layouts re-verify the session and role from `public.users` server-side on every render (`supabase.auth.getUser()` + role lookup + redirect). This plan does not touch that authorization logic — only the JSX grouping of already-rendered, already-authorized utility controls after the existing role checks pass. |
| None new | No new server action, API route, or database query is introduced. The restructure only changes how `ThemeToggle`, `NotificationBell`, and `LogoutButton` are grouped in JSX — all three remain the same components with the same props (`userId={userData.user.id}` for `NotificationBell` is unchanged). `RoleNavLinks`/`RoleMobileNavLinks` and the branded header are not modified by this plan at all. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03.1-05 | Tampering | Layout JSX restructure (farmer/owner) | accept | Purely a rearrangement of existing, already-authorized UI elements into separate visual groupings (one flat div -> two grouped divs). No new data is fetched, no new write path is introduced, and the pre-existing server-side role redirect logic (`if (profile.role === "owner") redirect(...)`) is preserved verbatim — it executes before the restructured JSX is ever returned. |
| T-03.1-06 | Denial of Service | `SheetClose`/`nativeButton={false}` pattern preserved (lives inside `RoleMobileNavLinks`, untouched) | accept | This plan does not modify `RoleMobileNavLinks` at all, so the Phase 02.1-06 fix for the base-ui Dialog Close primitive bug (mobile nav drawer close-on-navigate) cannot regress — the component implementing that fix is out of scope for this plan's edits. |
| T-03.1-SC | Tampering | No new package installs in this plan | accept | This plan modifies only existing first-party layout files. No `npm install` / `pip install` / `cargo add` commands are run. Package Legitimacy Gate not applicable. |
</threat_model>

<verification>
- Both `(farmer)/layout.tsx` and `(owner)/layout.tsx` contain `mt-auto` at least once (the bottom-pinned logout wrapper).
- `ThemeToggle` and `NotificationBell` appear inside the same parent div in both the desktop header and the mobile Sheet's new utility row, in both files.
- `LogoutButton` appears inside its own div, separate from the `ThemeToggle`/`NotificationBell` div, in both desktop and mobile, in both files.
- `RoleNavLinks`, `RoleMobileNavLinks`, `AgriMateAIIcon` imports and usages are byte-for-byte unchanged from pre-task state.
- Manual/human-deferred check (covered by Phase 3.1's overall human-verify step): resize browser below 640px and confirm the Sheet drawer shows a distinct utility row (theme+bell) separate from the bottom logout row; resize above 640px and confirm the header shows a visually distinct utility cluster separate from logout; navigate between farmer/owner dashboard, browse, favorites, chat and confirm nav links still route correctly and active-link highlighting still works.
</verification>

<success_criteria>
- Farmer and owner layouts both show `ThemeToggle`+`NotificationBell` as one visually distinct cluster, separate from a separately-grouped `LogoutButton`, on both desktop header and mobile Sheet drawer.
- `LogoutButton` is bottom-pinned via `mt-auto` in the mobile Sheet drawer in both layouts.
- The existing branded header, `RoleNavLinks`/`RoleMobileNavLinks` active-link highlighting, and `SheetClose`/`nativeButton={false}` close-on-navigate fix are unregressed.
- `npm run build` (or `next build`) completes without TypeScript/lint errors introduced by these changes.
</success_criteria>

<output>
Create `.planning/phases/03.1-auth-navigation-ui-fixes-inserted/03.1-03-SUMMARY.md` when done
</output>

## Artifacts this phase produces

- Desktop utility-cluster wrapper div (`flex items-center gap-2 rounded-full border border-border/70 bg-muted/30 p-1`) grouping `ThemeToggle`+`NotificationBell`, and a separate bordered div grouping `LogoutButton`, in both `src/app/(farmer)/layout.tsx` and `src/app/(owner)/layout.tsx`'s desktop header.
- New mobile Sheet utility row (`flex items-center justify-between gap-2 border-b border-border/70 px-4 py-3`) containing `ThemeToggle`+`NotificationBell`, inserted between `SheetHeader` and the `RoleMobileNavLinks`-driven nav block, in both layout files.
- Restructured bottom Sheet zone (`mt-auto border-t border-border/70 px-4 py-4`) now containing only `LogoutButton` (ThemeToggle/NotificationBell removed from there, relocated to the new top utility row), in both layout files.
