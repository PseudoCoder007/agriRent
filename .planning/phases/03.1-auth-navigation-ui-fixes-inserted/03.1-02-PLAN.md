---
phase: 03.1
plan: 02
type: execute
wave: 2
depends_on: ["03.1-01"]
files_modified:
  - "src/app/(auth)/login/page.tsx"
  - "src/app/(auth)/signup/page.tsx"
  - src/components/auth/google-oauth-button.tsx
autonomous: true
requirements: [UI-AUTH-02]

must_haves:
  truths:
    - "Login and signup pages have a card border that is visually more defined than the prior near-invisible default border token"
    - "The Google OAuth button border is unambiguously visible standalone on the white card, independent of the .force-light fix"
    - "Form labels render at font-semibold (600 weight), matching the phase's 2-weight typography contract"
    - "The submit button remains the highest-contrast, most visually dominant element on the card (primary focal point); the Google button stays a secondary, lower-contrast anchor"
  artifacts:
    - path: "src/app/(auth)/login/page.tsx"
      provides: "border-slate-200 card border, font-semibold FormLabel styling"
    - path: "src/app/(auth)/signup/page.tsx"
      provides: "border-slate-200 card border, font-semibold FormLabel styling"
    - path: "src/components/auth/google-oauth-button.tsx"
      provides: "border-slate-400 button border (escalated from border-slate-300 for standalone visibility)"
  key_links:
    - from: "src/app/(auth)/login/page.tsx"
      to: "src/components/auth/google-oauth-button.tsx"
      via: "<GoogleOAuthButton mode=\"login\" /> rendered inside the .force-light card"
      pattern: "GoogleOAuthButton"
---

<objective>
Apply the visual redesign contract from 03.1-UI-SPEC.md to the login and signup pages: upgrade the card border for clearer definition, collapse form-label weight to the phase's semibold standard, and escalate the Google OAuth button border for standalone visibility — all while keeping the existing shared structure (centered card: Google button -> divider -> form fields -> submit -> cross-link) unchanged, per the spec's explicit "no structural rebuild" instruction.

Purpose: Per UI-AUTH-02, login/signup must have "a polished, cohesive visual design, not just bug-fixed defaults." This plan executes the spec's concrete redesign table (card border, Google button contrast, label typography) without introducing new component primitives or restructuring the form layout.

Output: Login and signup pages with `border-slate-200` card borders and `font-semibold` form labels; Google OAuth button with an escalated, standalone-visible border.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03.1-auth-navigation-ui-fixes-inserted/03.1-UI-SPEC.md
@.planning/phases/03.1-auth-navigation-ui-fixes-inserted/03.1-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade card border and form label weight on login/signup</name>
  <files>src/app/(auth)/login/page.tsx, src/app/(auth)/signup/page.tsx</files>
  <read_first>
    Read both files in their post-Plan-03.1-01 state: `force-light` already applied to `<main>`, and the `dark:bg-slate-900/70 dark:text-slate-50` variant classes already stripped from the form inputs by Plan 03.1-01 (the inputs otherwise keep an in-progress redesign's styling — `h-11 rounded-xl border-slate-300 bg-slate-50/95 px-4 text-slate-950 shadow-sm placeholder:text-slate-400 focus-visible:border-emerald-500 focus-visible:ring-emerald-500/20` — untouched by this plan; this task does not touch the inputs at all). Both files share the identical card wrapper: `<div className="rounded-xl border bg-white p-6 shadow-sm sm:p-8">` (unaffected by Plan 03.1-01, still in its original form). Both use `<FormLabel>` from `@/components/ui/form` with no explicit className today (inherits the shadcn default weight). 03.1-UI-SPEC.md's Color section specifies escalating the card border from the default `border` token to `border-slate-200`; the Typography section specifies `FormLabel` should render at `font-semibold` (600) for this phase's redesigned auth pages, changed from the prior inherited/medium weight.
  </read_first>
  <action>
    On both `login/page.tsx` and `signup/page.tsx`, change the card wrapper's className from `"rounded-xl border bg-white p-6 shadow-sm sm:p-8"` to `"rounded-xl border border-slate-200 bg-white p-6 shadow-sm sm:p-8"` (add `border-slate-200` alongside the existing `border` utility — `border` sets width/style, `border-slate-200` sets the color, both are needed since Tailwind's bare `border` utility only sets a 1px solid border and relies on `border-color` separately). On every `<FormLabel>` element in both files (login: Email, Password — 2 occurrences; signup: Full name, Email, Password, "I am a" — 4 occurrences), add `className="font-semibold"` so each reads `<FormLabel className="font-semibold">...</FormLabel>`. Do not change `FormLabel`'s own component source in `@/components/ui/form` — this is a per-usage className override, keeping the shared component untouched per the phase's out-of-scope instruction. Do not touch the `Input`/`PasswordInput` className strings — those were already finalized by Plan 03.1-01 and are out of scope for this task.
  </action>
  <acceptance_criteria>
    - Both files' card wrapper div contains `border-slate-200` in its className alongside the existing `border` class
    - Every `<FormLabel>` in login/page.tsx (2 total) has `className="font-semibold"`
    - Every `<FormLabel>` in signup/page.tsx (4 total, including the "I am a" role label) has `className="font-semibold"`
    - No other className on either file's card wrapper or FormLabel elements is altered
    - `Input`/`PasswordInput` className strings are byte-for-byte unchanged from their post-03.1-01 state
  </acceptance_criteria>
  <verify>
    <automated>grep -c "border-slate-200" "src/app/(auth)/login/page.tsx" "src/app/(auth)/signup/page.tsx"</automated>
  </verify>
  <done>Both pages' card borders use `border-slate-200`; all FormLabel elements carry `font-semibold`.</done>
</task>

<task type="auto">
  <name>Task 2: Escalate Google OAuth button border contrast</name>
  <files>src/components/auth/google-oauth-button.tsx</files>
  <read_first>
    Read src/components/auth/google-oauth-button.tsx (57 lines). The button's className today is `"h-11 w-full rounded-full border-slate-300 bg-white text-slate-950 hover:bg-slate-50"`. 03.1-UI-SPEC.md's Color section root-cause table confirms this border is a hardcoded literal (not a CSS var, not affected by `.force-light`), and notes `border-slate-300` against `bg-white` is only ~1.3:1 contrast — the spec's escalation fallback is `border-slate-400` if visual judgment at implementation time confirms `border-slate-300` reads as too weak standalone.
  </read_first>
  <action>
    In `src/components/auth/google-oauth-button.tsx`, change the `Button` element's className from `"h-11 w-full rounded-full border-slate-300 bg-white text-slate-950 hover:bg-slate-50"` to `"h-11 w-full rounded-full border-slate-400 bg-white text-slate-950 hover:bg-slate-50"` (the spec's escalation fallback, applied directly since `border-slate-300` is documented in the spec itself as low-contrast and the user already flagged this border as invisible). Keep `rounded-full`, `h-11`, and all other classes unchanged — only the border color step changes from `slate-300` to `slate-400`. Do not modify the `<span>` "G" badge's `ring-1 ring-slate-200` (unrelated to the button's own border, out of scope).
  </action>
  <acceptance_criteria>
    - `src/components/auth/google-oauth-button.tsx`'s `Button` className contains `border-slate-400` and does not contain `border-slate-300`
    - `rounded-full`, `h-11`, `w-full`, `bg-white`, `text-slate-950`, `hover:bg-slate-50` remain present and unchanged
    - The "G" badge span's `ring-1 ring-slate-200` is untouched
  </acceptance_criteria>
  <verify>
    <automated>grep -c "border-slate-400" src/components/auth/google-oauth-button.tsx</automated>
  </verify>
  <done>Google OAuth button border is `border-slate-400`; no `border-slate-300` remains on the button itself.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None new | This plan changes only Tailwind className strings on existing JSX elements (card border color, label font weight, button border color). No new server action, API route, database query, or auth surface is introduced. The Google OAuth flow itself (`supabase.auth.signInWithOAuth`) is untouched — only its trigger button's visual border color changes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03.1-03 | Spoofing | Google OAuth button border color change | accept | Purely cosmetic border-color escalation (`border-slate-300` -> `border-slate-400`). Does not change the button's `onClick` handler, the `redirectTo` URL construction, or any OAuth parameter — `handleGoogleLogin` and `buildRedirectUrl` in google-oauth-button.tsx are unmodified by this plan. No spoofing/phishing surface is introduced or altered. |
| T-03.1-04 | Information Disclosure | FormLabel font-weight className addition | accept | `className="font-semibold"` only affects rendered text weight, not text content. No information is added, removed, or conditionally revealed. |
| T-03.1-SC | Tampering | No new package installs in this plan | accept | This plan modifies only existing first-party files (2 page components, 1 button component). No `npm install` / `pip install` / `cargo add` commands are run. Package Legitimacy Gate not applicable. |
</threat_model>

<verification>
- `grep -c "border-slate-200"` on login/page.tsx and signup/page.tsx each return at least 1.
- `grep -c "border-slate-400"` on google-oauth-button.tsx returns 1; `border-slate-300` no longer appears in that file.
- All FormLabel elements on both pages carry `className="font-semibold"` (2 on login, 4 on signup).
- Manual/human-deferred check (covered by Phase 3.1's overall human-verify step): visually confirm the Google button border is clearly visible against the white card in light mode, and that the submit button still reads as the highest-contrast element on the card.
</verification>

<success_criteria>
- Login and signup card borders render at `border-slate-200`.
- All FormLabel elements on both pages render at `font-semibold`.
- Google OAuth button border renders at `border-slate-400`, visibly distinct from the white card background.
- `npm run build` (or `next build`) completes without TypeScript/lint errors introduced by these changes.
</success_criteria>

<output>
Create `.planning/phases/03.1-auth-navigation-ui-fixes-inserted/03.1-02-SUMMARY.md` when done
</output>

## Artifacts this phase produces

- `border-slate-200` className addition on the login and signup card wrapper `<div>` elements.
- `font-semibold` className addition on all `FormLabel` usages within login (2) and signup (4) pages.
- `border-slate-400` className change (replacing `border-slate-300`) on the `Button` element inside `src/components/auth/google-oauth-button.tsx`.
