---
phase: 03.5-home-page-feature-showcase
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/page.tsx
autonomous: false
requirements: []

must_haves:
  truths:
    - "A visitor on the home page sees a feature grid of exactly 6 cards describing equipment browsing/booking, owner listing management, database-enforced no-double-booking, reviews, live notifications/email, and AgriMate AI — not the old stale generic copy"
    - "The feature grid no longer references the removed 'For Indian farms' / Trees-icon card"
    - "The hero, CTA banner, and footer sections remain functionally and visually unchanged (no regression to existing accurate copy)"
    - "Typography weight consolidation (hero headline to font-semibold, feature card title to font-semibold) is applied per UI-SPEC"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "Updated landing page features array (6 entries) with Bell icon import, Trees import removed, consolidated font weights"
      contains: "Bell"
  key_links:
    - from: "src/app/page.tsx features array"
      to: "lucide-react icon imports"
      via: "import statement"
      pattern: "import \\{ Search, ListCheck, MessageSquareText, Star, ShieldCheck, Bell \\} from \"lucide-react\""
---

<objective>
Refresh the landing page's feature grid copy and icon set in `src/app/page.tsx` so it accurately represents AgriRent's shipped functionality today (equipment browse/booking, owner listing management, DB-enforced no-double-booking, reviews, live notifications + branded email, AgriMate AI chat) instead of the pre-existing stale/generic 6-card grid that predates most of these features.

Purpose: The landing page is the first thing a visitor sees; outdated copy undersells the product and omits major shipped capabilities (reviews, notifications, AI branding) that exist today per `.planning/phases/03.5-home-page-feature-showcase/03.5-UI-SPEC.md`.
Output: Updated `src/app/page.tsx` with a rewritten `features` array (6 entries, new copy per the UI-SPEC's Copywriting Contract), `Bell` icon imported and `Trees` icon removed, and the two typography weight consolidations applied (hero headline `font-bold` → `font-semibold`; feature card title `font-medium` → `font-semibold`). Hero, CTA banner, and footer sections are otherwise unchanged.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03.5-home-page-feature-showcase/03.5-UI-SPEC.md
@src/app/page.tsx
@src/components/brand/agri-mate-ai-mark.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite feature grid content and icon imports in src/app/page.tsx</name>
  <files>src/app/page.tsx</files>
  <read_first>
    src/app/page.tsx (full file — current 189-line implementation, `features` array at lines 6-37, icon import at line 1)
    .planning/phases/03.5-home-page-feature-showcase/03.5-UI-SPEC.md (Section "Feature grid section — REWRITE required", "Feature Icon Inventory", "Copywriting Contract" tables — exact copy source of truth)
    src/components/brand/agri-mate-ai-mark.tsx (confirms `AgriMateAIIcon` accepts `variant="navbar" | "avatar" | "favicon"` and `className` prop, default export is a named export `AgriMateAIIcon`)
  </read_first>
  <action>
    In the icon import on line 1, remove `Trees` and add `Bell`, producing exactly: `import { Search, ListCheck, MessageSquareText, Star, ShieldCheck, Bell } from "lucide-react";`. If using the `AgriMateAIIcon` component for card 6 (see below), also add `import { AgriMateAIIcon } from "@/components/brand/agri-mate-ai-mark";` as a new import line.

    Replace the entire `features` array (current lines 6-37) with exactly 6 entries matching the UI-SPEC's Copywriting Contract table verbatim:
    1. `icon: Search`, `title: "Browse & book"`, `description: "Search tractors, harvesters, and more by category or location, then request a date range — pricing is calculated automatically."`
    2. `icon: ListCheck`, `title: "Owners stay in control"`, `description: "List equipment in minutes and approve or reject every booking request from a dedicated dashboard."`
    3. `icon: ShieldCheck`, `title: "No double-booking"`, `description: "Availability is enforced at the database level on every request — never just a UI check."`
    4. `icon: Star`, `title: "Real reviews"`, `description: "Farmers rate and review equipment after every completed rental, visible on the listing page."`
    5. `icon: Bell`, `title: "Live notifications"`, `description: "A real-time notification bell and branded email alerts keep both sides updated the moment a booking status changes."`
    6. `icon: MessageSquareText`, `title: "AgriMate AI"`, `description: "Ask AgriMate AI rental questions any time — booking policies, equipment tips, and more, answered instantly."`

    For card 6's icon rendering specifically: per UI-SPEC's recommendation, special-case the render so the icon-chip wrapper (`<div className="mb-3 flex h-10 w-10 items-center justify-center rounded-lg bg-emerald-100 text-emerald-700">`) renders `<AgriMateAIIcon variant="navbar" className="h-5 w-5" />` when `feature.title === "AgriMate AI"`, and `<feature.icon className="h-5 w-5" />` for all other cards. Keep `icon: MessageSquareText` in the array entry for card 6 as an unused fallback/type-consistency value (the array's `icon` field type must stay a uniform lucide `ComponentType`, so do not change the array's shape — branch only inside the JSX render, e.g. `{feature.title === "AgriMate AI" ? <AgriMateAIIcon variant="navbar" className="h-5 w-5" /> : <feature.icon className="h-5 w-5" />}`).

    Do not change the section heading ("Everything you need") or subhead ("From discovery to booking to feedback — all in one place.") — both already accurate per UI-SPEC.

    Do not change the hero section's headline text, subhead text, CTA banner text/buttons, or footer text — all confirmed accurate and unchanged per UI-SPEC Section-by-Section Content Contract items 1, 4, 5.

    Apply the two typography weight consolidations specified in the UI-SPEC's Typography table: change the hero `<h1>` className's `font-bold` to `font-semibold` (line 58 currently reads `className="max-w-2xl text-4xl font-bold tracking-tight text-slate-950 sm:text-5xl lg:text-6xl"`), and change the feature card `<h3>` className's `font-medium` to `font-semibold` (line 148 currently reads `className="mb-1 font-medium text-slate-950"`).

    Do not touch the hero panel's `/globe.svg` icon (UI-SPEC marks this optional/Claude's Discretion, not required — leave as-is per UI-SPEC's stated default).

    Do not add a new "How it works" section — UI-SPEC marks this optional/not required for satisfying the phase goal; the rewritten feature grid alone satisfies the accuracy requirement.
  </action>
  <acceptance_criteria>
    src/app/page.tsx line 1 import statement contains `Bell` and does not contain `Trees`.
    src/app/page.tsx `features` array contains exactly 6 objects, with `title` values in order: "Browse & book", "Owners stay in control", "No double-booking", "Real reviews", "Live notifications", "AgriMate AI".
    No occurrence of the string "Trees" or "For Indian farms" remains anywhere in src/app/page.tsx.
    No occurrence of the old stale descriptions ("Get recommendations and answers instantly.", "Built for local equipment and rental practices.") remains in src/app/page.tsx.
    The hero `<h1>` className contains `font-semibold` and does not contain `font-bold`.
    The feature card `<h3>` className contains `font-semibold` and does not contain `font-medium`.
    Hero headline text "Rent farm equipment" and "without the friction." are unchanged in the file.
    CTA banner heading "Ready to get started?" and footer text "AgriRent — farm equipment rental marketplace" are unchanged in the file.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>src/app/page.tsx compiles with no TypeScript errors, contains the rewritten 6-card feature grid with new copy and Bell icon, and the two typography consolidations are applied with hero/CTA/footer sections otherwise unchanged.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Rewritten home page feature grid (6 cards: Browse & book, Owners stay in control, No double-booking, Real reviews, Live notifications, AgriMate AI) with new Bell icon, AgriMate AI brand mark on card 6, and consolidated semibold typography on the hero headline and feature card titles.</what-built>
  <how-to-verify>
    1. Run `npm run dev` and visit `http://localhost:3000/` in a browser (or visit the deployed `https://agrirent.shop` if already deployed).
    2. Confirm the hero section is unchanged: "Rent farm equipment without the friction." headline, subhead, "Get started"/"Log in" buttons, and the farmer/owner two-tone split card.
    3. Scroll to the "Everything you need" feature grid and confirm exactly 6 cards in this order: Browse & book, Owners stay in control, No double-booking, Real reviews, Live notifications, AgriMate AI.
    4. Confirm the "Live notifications" card shows a bell icon and the "AgriMate AI" card shows the green leaf-shaped AgriMate AI brand mark (not a generic chat bubble icon, not the old 🌳 Trees icon).
    5. Confirm the CTA banner ("Ready to get started?") and footer ("AgriRent — farm equipment rental marketplace") are unchanged.
    6. Confirm the page renders force-light (white/emerald gradient background) regardless of your current dark-mode toggle preference.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None new | This phase is a static-content edit to one Server Component page. No new user input, no new data writes, no new auth surface, no new external request. The only existing dynamic behavior on this page (the `code` searchParam → `/auth/callback` redirect) is untouched by this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-------------------|
| T-03.5-01 | Tampering | src/app/page.tsx static JSX copy | accept | All copy is hardcoded string literals in server-rendered JSX, not derived from any user input or external source — no injection surface exists. No mitigation needed beyond standard JSX auto-escaping (which React provides by default for all text content). |
| T-03.5-02 | Information Disclosure | src/app/page.tsx feature descriptions | accept | New copy describes only already-public, already-shipped functionality (reviews, notifications, AI chat) — no internal implementation detail, secret, or unreleased-feature disclosure. |
| T-03.5-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan — `Bell` icon and `AgriMateAIIcon` are both already-installed dependencies (lucide-react, existing local component). Package Legitimacy Gate not triggered. |
</threat_model>

<verification>
- `npx tsc --noEmit -p tsconfig.json` passes with no errors introduced by this change.
- Manual visual check per the checkpoint task confirms 6-card grid order, icon set, and unchanged hero/CTA/footer sections.
- `grep -v '^import' src/app/page.tsx | grep -c Trees` returns 0 (Trees fully removed from non-import lines; import line itself no longer references Trees either per acceptance criteria).
</verification>

<success_criteria>
- src/app/page.tsx feature grid shows exactly 6 cards reflecting equipment browsing/booking, owner listing management, DB-enforced no-double-booking, reviews, live notifications + email, and AgriMate AI.
- No stale references to "For Indian farms" or generic "Get recommendations and answers instantly" copy remain.
- Hero, CTA banner, and footer sections are visually and textually unchanged from before this plan.
- TypeScript compiles cleanly; dev server renders the page without errors.
</success_criteria>

<output>
Create `.planning/phases/03.5-home-page-feature-showcase/03.5-01-SUMMARY.md` when done
</output>
