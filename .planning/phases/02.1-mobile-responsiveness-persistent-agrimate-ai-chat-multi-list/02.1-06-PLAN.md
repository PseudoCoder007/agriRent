---
phase: 02.1-mobile-responsiveness-persistent-agrimate-ai-chat-multi-list
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(farmer)/layout.tsx
  - src/app/(owner)/layout.tsx
autonomous: false
gap_closure: true
requirements: [MOBILE-01]

must_haves:
  truths:
    - "On viewports narrower than 640px, opening the hamburger drawer and tapping any nav link closes the drawer and navigates to the destination page (no stale open drawer/backdrop left over the new page)"
    - "The mobile nav Sheet drawer exposes an accessible title to screen readers (no unlabelled dialog region)"
    - "At 640px and above, the existing horizontal nav and inline header controls are unaffected by this fix"
  artifacts:
    - path: "src/app/(farmer)/layout.tsx"
      provides: "Sheet drawer nav Links wrapped in SheetClose so the drawer closes on navigation; SheetTitle present for accessibility"
      contains: "SheetClose"
    - path: "src/app/(owner)/layout.tsx"
      provides: "Sheet drawer nav Links wrapped in SheetClose so the drawer closes on navigation; SheetTitle present for accessibility"
      contains: "SheetClose"
  key_links:
    - from: "src/app/(farmer)/layout.tsx"
      to: "src/components/ui/sheet.tsx"
      via: "SheetClose render prop wrapping each nav Link"
      pattern: "SheetClose"
    - from: "src/app/(owner)/layout.tsx"
      to: "src/components/ui/sheet.tsx"
      via: "SheetClose render prop wrapping each nav Link"
      pattern: "SheetClose"
---

<objective>
Close gap MOBILE-01 (CR-01 / VERIFICATION.md gap): the mobile nav Sheet drawer in both src/app/(farmer)/layout.tsx and src/app/(owner)/layout.tsx is uncontrolled and its nav Links have no close-on-navigate wiring, so the drawer stays visibly open over the destination page after a user taps a nav link (these layouts are not remounted by the App Router across sibling-route navigation). Fix by wrapping each nav Link in SheetClose (base-ui render prop pattern, matching the existing SheetTrigger usage already in both files -- this codebase's Sheet is @base-ui/react/dialog-backed, not Radix, so asChild does not exist; render is the correct prop). While touching this exact block, also fix WR-06 (missing SheetTitle/SheetDescription, a real accessibility regression on the same dialog instance).

Purpose: MOBILE-01 (per ROADMAP Phase 2.1 requirement) requires the mobile nav collapse to be a clean, working navigation pattern, not just a structurally-present drawer. A drawer that fails to close on navigation is a shipped, user-visible breakage of the exact flow this requirement exists to fix -- VERIFICATION.md scored this PARTIAL specifically because of this defect.
Output: Both layouts' Sheet drawer nav Links close the drawer automatically on tap-to-navigate, and the drawer carries a screen-reader-accessible title.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02.1-mobile-responsiveness-persistent-agrimate-ai-chat-multi-list/02.1-VERIFICATION.md
@.planning/phases/02.1-mobile-responsiveness-persistent-agrimate-ai-chat-multi-list/02.1-REVIEW.md
@src/components/ui/sheet.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wrap farmer layout's Sheet drawer nav Links in SheetClose and add SheetTitle</name>
  <files>src/app/(farmer)/layout.tsx</files>
  <read_first>
    src/app/(farmer)/layout.tsx (current state -- SheetContent block at lines 82-103: bare nav Links with no close wiring, no SheetTitle)
    src/components/ui/sheet.tsx (SheetClose at line 18 accepts SheetPrimitive.Close.Props, which includes a render prop -- same API shape as SheetTrigger, already used correctly elsewhere in this same file at line 70-78; SheetTitle at line 103 renders an accessible dialog title)
  </read_first>
  <action>
    In src/app/(farmer)/layout.tsx, add SheetClose and SheetTitle to the existing import { Sheet, SheetContent, SheetTrigger } from "@/components/ui/sheet"; line. Inside SheetContent, immediately after the opening tag and before the nav block, add a SheetTitle element with className "sr-only" and text "Navigation menu" (visually hidden, matches WR-06's documented fix and Shadcn's own pattern for icon-only/menu sheets). Then wrap each of the four nav Link elements (Dashboard, Browse, Favorites, Chat) in SheetClose using the render prop -- the same pattern already used for SheetTrigger two blocks above in this file (render={{Button .../}}) -- so each Link becomes a child of a SheetClose whose render prop is that exact Link element (preserving each Link's existing href and className unchanged), with the visible label text moved to be SheetClose's children instead of the Link's children. Do not wrap or change the footer controls (ThemeToggle, NotificationBell, LogoutButton) -- those are not navigation links and are out of scope for this gap (the gap is specifically about nav Links not closing the drawer). Do not introduce controlled open/onOpenChange state -- SheetClose's render-prop wrapping is base-ui's built-in close-on-activate mechanism and is sufficient; do not add unnecessary complexity per CLAUDE.md code-quality conventions.
  </action>
  <acceptance_criteria>
    <source_assertion>src/app/(farmer)/layout.tsx imports SheetClose and SheetTitle from "@/components/ui/sheet"</source_assertion>
    <source_assertion>src/app/(farmer)/layout.tsx's SheetContent block wraps all four nav Links (/farmer/dashboard, /browse, /farmer/favorites, /farmer/chat) in SheetClose using the render prop</source_assertion>
    <source_assertion>src/app/(farmer)/layout.tsx's SheetContent block contains a SheetTitle with className "sr-only"</source_assertion>
    <test_command>npx tsc --noEmit 2>&1 | grep -i "layout.tsx" || echo "no layout.tsx type errors"</test_command>
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -i "layout.tsx" || echo "no layout.tsx type errors"</automated>
  </verify>
  <done>Farmer layout's mobile drawer nav Links are each wrapped in SheetClose (render prop), a sr-only SheetTitle is present, and the file type-checks cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Wrap owner layout's Sheet drawer nav Links in SheetClose and add SheetTitle</name>
  <files>src/app/(owner)/layout.tsx</files>
  <read_first>
    src/app/(owner)/layout.tsx (current state -- SheetContent block at lines 76-91: bare nav Links with no close wiring, no SheetTitle)
  </read_first>
  <action>
    Apply the identical structural change as Task 1 to src/app/(owner)/layout.tsx: add SheetClose and SheetTitle to the existing import { Sheet, SheetContent, SheetTrigger } from "@/components/ui/sheet"; line, add a sr-only SheetTitle ("Navigation menu") immediately after SheetContent's opening tag and before the nav block, then wrap each of the two nav Link elements (Dashboard, Chat) in SheetClose using the render prop, preserving each Link's exact href and className unchanged and moving the visible label text to be SheetClose's children. Do not wrap or change the footer controls (ThemeToggle, NotificationBell, LogoutButton).
  </action>
  <acceptance_criteria>
    <source_assertion>src/app/(owner)/layout.tsx imports SheetClose and SheetTitle from "@/components/ui/sheet"</source_assertion>
    <source_assertion>src/app/(owner)/layout.tsx's SheetContent block wraps both nav Links (/owner/dashboard, /owner/chat) in SheetClose using the render prop</source_assertion>
    <source_assertion>src/app/(owner)/layout.tsx's SheetContent block contains a SheetTitle with className "sr-only"</source_assertion>
    <test_command>npx tsc --noEmit 2>&1 | grep -i "layout.tsx" || echo "no layout.tsx type errors"</test_command>
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -i "layout.tsx" || echo "no layout.tsx type errors"</automated>
  </verify>
  <done>Owner layout's mobile drawer nav Links are each wrapped in SheetClose (render prop), a sr-only SheetTitle is present, and the file type-checks cleanly.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Both farmer and owner mobile nav drawers now close automatically when a nav link is tapped (SheetClose render-prop wiring), and both drawers carry a screen-reader-accessible title.</what-built>
  <how-to-verify>
    1. Run the dev server (npm run dev), open devtools, set viewport to 375px width.
    2. Log in as a farmer, open the hamburger drawer on /farmer/dashboard, tap "Chat" -- confirm the drawer closes immediately and you land on /farmer/chat with no leftover overlay/backdrop.
    3. Repeat for "Dashboard", "Browse", and "Favorites" links in the farmer drawer -- each should close the drawer and navigate cleanly.
    4. Log in as an owner, open the hamburger drawer on /owner/dashboard, tap "Chat" -- confirm the drawer closes and you land on /owner/chat cleanly. Repeat for "Dashboard".
    5. Resize back to >=640px on both roles -- confirm the horizontal nav and inline header controls render exactly as before (no regression).
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None new | Pure client-side navigation UI fix; no new data flow, no new authorization surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.1-07 | None applicable | Sheet drawer SheetClose wiring | accept | base-ui's Dialog.Close under the hood already handles focus restoration/ARIA correctly; no custom security-relevant logic introduced by wrapping existing Links |
</threat_model>

<verification>
- npx tsc --noEmit passes with no errors in either layout file
- Manual (human checkpoint): opening the drawer and tapping any nav link on both farmer and owner layouts closes the drawer and navigates cleanly, at <640px
- Manual (human checkpoint): desktop (>=640px) horizontal nav and inline controls are unchanged
</verification>

<success_criteria>
- Both farmer and owner mobile nav Sheet drawers close automatically when any nav link is tapped
- Both drawers expose a screen-reader-accessible SheetTitle (WR-06 closed as a side effect of touching the same component instance)
- No regression to desktop-width (>=640px) header behavior
- MOBILE-01 truth "drawer closes on navigation" (VERIFICATION.md gap) is satisfied
</success_criteria>

<output>
Create `.planning/phases/02.1-mobile-responsiveness-persistent-agrimate-ai-chat-multi-list/02.1-06-SUMMARY.md` when done
</output>
