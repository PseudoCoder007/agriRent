---
phase: 02.1-mobile-responsiveness-persistent-agrimate-ai-chat-multi-list
plan: 01
subsystem: database
tags: [supabase, rls, postgres, zod, vitest, server-actions, chat-persistence]

# Dependency graph
requires:
  - phase: 01-walking-skeleton
    provides: notification.service.ts / favorites.actions.ts ServiceResult/ActionResult conventions, NVIDIA NIM chat widget (chat-widget.tsx, /api/chat route)
provides:
  - "Live chat_messages table on Supabase with own-row RLS (SELECT/INSERT only, no UPDATE/DELETE)"
  - "saveChatMessageSchema Zod validator (role enum + content .max(2000) DoS cap)"
  - "chat.service.ts: getChatHistory(userId), saveChatMessage(userId, role, content) -- RLS-respecting createClient() only, never createAdminClient"
  - "chat.actions.ts: saveChatMessageAction, getChatHistoryAction Server Actions with auth guard + Zod safeParse"
  - "Regenerated types/database.ts including Tables<\"chat_messages\">"
affects: [02.1-05 (chat widget wiring -- can now import getChatHistoryAction/saveChatMessageAction without further backend work)]

# Tech tracking
tech-stack:
  added: []
  patterns: ["own-row RLS chat persistence mirroring notifications/favorites/reviews migration shape", "TDD RED/GREEN for service-layer unit tests using vi.mock(\"@/lib/supabase/server\")"]

key-files:
  created:
    - supabase/migrations/0007_chat_messages.sql
    - src/lib/validations/chat.schema.ts
    - src/lib/services/chat.service.ts
    - src/lib/services/chat.service.test.ts
    - src/app/actions/chat.actions.ts
  modified:
    - types/database.ts

key-decisions:
  - "Single flat chat_messages table (no separate conversations parent) -- matches current one-thread-per-user UI, per RESEARCH.md Assumption A1"
  - "No UPDATE/DELETE RLS policies -- messages are immutable once written, matching reviews/notifications precedent"
  - "chat_messages NOT added to supabase_realtime publication -- history loads once per page mount (Server Component fetch), not subscribed live"
  - "Regenerated types/database.ts left as UTF-16LE (Windows PowerShell default) per RESEARCH.md Pitfall 5 -- not 'fixed', as that was explicitly deferred optional cleanup"

patterns-established:
  - "Pattern: own-row chat persistence -- user_id = (select auth.uid()) wrapped form for RLS perf, index on (user_id, created_at) matching policy reference columns"
  - "Pattern: chat.service.ts never uses createAdminClient -- chat is always same-user read/write, unlike notifications which cross users"

requirements-completed: [CHAT-PERSIST-01]

# Metrics
duration: ~25min (this continuation session; Task 1 + initial Task 2 push happened in a prior session)
completed: 2026-06-27
---

# Phase 02.1 Plan 01: Chat Persistence Backend Summary

**Live `chat_messages` table with own-row RLS, regenerated database types, and a tested `chat.service.ts`/`chat.actions.ts` pair mirroring the existing notifications/favorites ServiceResult/ActionResult conventions.**

## Performance

- **Duration:** ~25 min (this continuation session)
- **Started:** 2026-06-27T07:00:00Z (approx, continuation start)
- **Completed:** 2026-06-27T07:15:22Z
- **Tasks:** 3/3 (Task 1 done in prior session, Task 2 checkpoint resolved by user + closed out here, Task 3 executed here)
- **Files modified:** 6 (1 migration, 1 Zod schema, 1 regenerated types file, 1 service, 1 Server Action file, 1 test file)

## Accomplishments
- Closed out Task 2's human-action checkpoint: verified migration `0007_chat_messages` is applied on the live, linked Supabase project (`npx supabase migration list --linked` shows Local 0007 | Remote 0007) and committed the regenerated `types/database.ts` (now includes `Tables<"chat_messages">` with the `chat_messages_user_id_fkey` foreign key).
- Built `chat.service.ts` (`getChatHistory`, `saveChatMessage`) and `chat.actions.ts` (`saveChatMessageAction`, `getChatHistoryAction`) via TDD RED/GREEN, following the exact `notification.service.ts`/`favorites.actions.ts` shape this codebase already established.
- Plan 05 (chat widget wiring) can now import `getChatHistoryAction`/`saveChatMessageAction` directly with zero further backend work, per the plan's stated success criteria.

## Task Commits

Each task was committed atomically:

1. **Task 1: Write chat_messages migration and create Zod schema** - `600ee94` (feat) -- completed in prior session
2. **Task 2: Push migration 0007 to live Supabase and regenerate types** - DB push done by user directly in their terminal (verified via `npx supabase migration list --linked`); types regeneration committed here as `c2abaef` (chore)
3. **Task 3: Build chat.service.ts, chat.actions.ts, and unit tests** - TDD cycle:
   - RED: `bd73b30` (test) -- 5 failing tests, chat.service.ts did not exist
   - GREEN: `456ae6e` (feat) -- chat.service.ts + chat.actions.ts implemented, all 5 tests pass

**Plan metadata:** (this commit, to follow)

## Files Created/Modified
- `supabase/migrations/0007_chat_messages.sql` - chat_messages table, own-row RLS (select/insert only), index on (user_id, created_at)
- `src/lib/validations/chat.schema.ts` - saveChatMessageSchema (role enum, content .max(2000))
- `types/database.ts` - regenerated to include Tables<"chat_messages"> (UTF-16LE, expected on this Windows environment)
- `src/lib/services/chat.service.ts` - getChatHistory(userId), saveChatMessage(userId, role, content), both via createClient() (never createAdminClient)
- `src/app/actions/chat.actions.ts` - saveChatMessageAction, getChatHistoryAction with auth guard + Zod safeParse, no revalidatePath
- `src/lib/services/chat.service.test.ts` - 5 unit tests covering success/error paths for both service functions, mocking @/lib/supabase/server per the auth.service.test.ts pattern

## Decisions Made
- Followed the plan's exact specified shapes for migration, schema, service, and actions -- no deviation from the prescribed RLS policy set, Zod cap, or function signatures.
- Did not re-run `npx supabase db push` (already done by the user; re-running was explicitly prohibited by the continuation instructions and is unnecessary given Supabase's idempotent migration tracking already confirms 0007 applied).
- Left `types/database.ts` UTF-16LE-encoded as-is per RESEARCH.md Pitfall 5 -- this is expected PowerShell behavior, not a bug, and "fixing" the encoding was explicitly out of scope for this plan.

## Deviations from Plan

None - plan executed exactly as written. Task 2's checkpoint was resolved by the user directly (DB push + type regen in their own terminal); this session verified the evidence and committed the resulting artifact, then proceeded to Task 3 exactly per spec.

## Issues Encountered

Found 4 pre-existing failing tests in `src/lib/services/ai.service.test.ts` (created in Phase 01-04, commit `af005b8`) during the full-suite `npx vitest run` pass after Task 3's GREEN commit. Root cause: `cookies()` called outside a request scope from `listing.service.ts`'s `getAllEquipment()`, invoked via `ai.service.ts`'s `buildSystemPrompt()`. This is unrelated to any file this plan touches (chat.service.ts, chat.actions.ts, chat.schema.ts, chat.service.test.ts, 0007_chat_messages.sql) and predates this plan entirely. Logged to `.planning/phases/02.1-mobile-responsiveness-persistent-agrimate-ai-chat-multi-list/deferred-items.md` per the executor's Scope Boundary rule -- not fixed here, flagged for a future debug/quick task or the next phase touching `ai.service.ts`.

## User Setup Required

None - no external service configuration required. The Supabase migration push and type regeneration (the only external-service-touching steps in this plan) were already completed by the user in their own terminal before this continuation began.

## Next Phase Readiness
- `chat_messages` table is live on Supabase with own-row RLS enforced (verified via `npx supabase migration list --linked`).
- `types/database.ts` reflects the new table.
- `chat.service.ts`/`chat.actions.ts` exist, follow established ServiceResult/ActionResult shapes, and have passing unit test coverage (`npx vitest run src/lib/services/chat.service.test.ts` -- 5/5 pass).
- Plan 05 (chat widget wiring) can import `getChatHistoryAction`/`saveChatMessageAction` immediately with no further backend work.
- Known unrelated pre-existing test failures in `ai.service.test.ts` are documented in `deferred-items.md` for future triage; they do not block this plan's completion or Plan 05's work.

---
*Phase: 02.1-mobile-responsiveness-persistent-agrimate-ai-chat-multi-list*
*Completed: 2026-06-27*

## Self-Check: PASSED

All claimed files verified present on disk (supabase/migrations/0007_chat_messages.sql,
src/lib/validations/chat.schema.ts, src/lib/services/chat.service.ts,
src/lib/services/chat.service.test.ts, src/app/actions/chat.actions.ts, this SUMMARY.md).
All claimed commits verified present in git log (600ee94, c2abaef, bd73b30, 456ae6e).
