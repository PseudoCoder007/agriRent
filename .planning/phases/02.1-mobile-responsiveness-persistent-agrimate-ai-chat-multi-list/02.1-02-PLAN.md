---
phase: 02.1-mobile-responsiveness-persistent-agrimate-ai-chat-multi-list
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(owner)/equipment/new/page.tsx
  - src/app/(owner)/owner/dashboard/page.tsx
  - src/app/(farmer)/farmer/dashboard/page.tsx
autonomous: true
requirements: [BUG-LISTING-01, BUG-IMAGE-01, MOBILE-02]

must_haves:
  truths:
    - "An owner can create a second (and any subsequent) equipment listing and land on /owner/dashboard seeing all of their listings, not bounced back to /browse"
    - "The owner dashboard 'My Equipment' section shows each listing's uploaded photo (or an explicit 'No photo' fallback), not text-only rows"
    - "Dashboard booking list rows (both owner and farmer) stack title/meta above badge/actions on narrow viewports instead of compressing into one cramped horizontal row"
    - "No new hardcoded slate-* color literal is introduced; the two existing empty-state CTA links convert to shadcn Button/token-based styling so they invert correctly under dark mode"
  artifacts:
    - path: "src/app/(owner)/equipment/new/page.tsx"
      provides: "post-create redirect to /owner/dashboard (was /browse)"
      contains: "router.push(\"/owner/dashboard\")"
    - path: "src/app/(owner)/owner/dashboard/page.tsx"
      provides: "equipment thumbnail render + responsive booking row + token-based empty-state CTA"
      contains: "getEquipmentImageUrl"
    - path: "src/app/(farmer)/farmer/dashboard/page.tsx"
      provides: "responsive booking row + token-based empty-state CTA"
      contains: "sm:flex-row sm:items-center sm:justify-between"
  key_links:
    - from: "src/app/(owner)/equipment/new/page.tsx"
      to: "/owner/dashboard"
      via: "router.push after successful createEquipmentAction"
      pattern: "router\\.push\\(\"/owner/dashboard\"\\)"
    - from: "src/app/(owner)/owner/dashboard/page.tsx"
      to: "listingService.getEquipmentImageUrl"
      via: "next/image src= call inside the My Equipment list"
      pattern: "listingService\\.getEquipmentImageUrl"
---

<objective>
Fix the two confirmed bugs (stale post-create redirect target; missing image render on the owner dashboard) and the dashboard-row mobile-responsiveness gap (MOBILE-02), plus convert the two hardcoded-color empty-state CTAs to shadcn tokens so they don't break under dark mode (Plan 03 wires dark mode globally — these CTAs must already be token-based by the time it ships).

Purpose: BUG-LISTING-01 and BUG-IMAGE-01 are both one-file, root-cause-confirmed fixes (see RESEARCH.md) — no schema or service-layer change needed. MOBILE-02 fixes the dashboard list rows that cram together below ~375px. Bundling all three in one plan is correct because they touch the same two dashboard files and the equipment/new page, with zero dependency on the chat/theme work in other plans.
Output: Owner can list equipment repeatedly without dead-ending; both dashboards show photos and wrap correctly on narrow viewports; empty-state CTAs use `bg-primary`/`text-primary-foreground` tokens.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02.1-mobile-responsiveness-persistent-agrimate-ai-chat-multi-list/02.1-RESEARCH.md
@.planning/phases/02.1-mobile-responsiveness-persistent-agrimate-ai-chat-multi-list/02.1-PATTERNS.md
@.planning/phases/02.1-mobile-responsiveness-persistent-agrimate-ai-chat-multi-list/02.1-UI-SPEC.md
@src/app/(farmer)/browse/page.tsx
@src/app/(owner)/equipment/[id]/edit/EditEquipmentForm.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix multi-listing redirect bug (BUG-LISTING-01)</name>
  <files>src/app/(owner)/equipment/new/page.tsx</files>
  <read_first>
    src/app/(owner)/equipment/new/page.tsx (current line 92: `router.push("/browse")`)
    src/app/(owner)/equipment/[id]/edit/EditEquipmentForm.tsx (line 82: working `router.push("/owner/dashboard")` analog)
  </read_first>
  <action>
    In `src/app/(owner)/equipment/new/page.tsx`, change line 92 from `router.push("/browse")` to `router.push("/owner/dashboard")`, leaving the preceding `toast.success(result.message);` on line 91 unchanged — the existing success toast already fires correctly and is the only "confirmation" UI required per UI-SPEC.md §5 (no new interstitial page). Do not touch the Zod schema (`createEquipmentSchema`), the `createEquipmentAction` call, or add any listing-count check — this is purely the redirect target, per RESEARCH.md Pitfall 3 / Anti-Patterns. Grep the rest of `(owner)/**` for any other stale `router.push("/browse")` call to confirm this is the only one (RESEARCH.md flagged this as worth re-checking at execution time since line numbers may have shifted).
  </action>
  <acceptance_criteria>
    <source_assertion>src/app/(owner)/equipment/new/page.tsx contains router.push("/owner/dashboard") and does NOT contain router.push("/browse")</source_assertion>
    <behavior_assertion>After submitting the new-equipment form successfully, the owner lands on /owner/dashboard, not /browse (manual browser check per VALIDATION.md — no e2e framework exists)</behavior_assertion>
  </acceptance_criteria>
  <verify>
    <automated>grep -c "router.push(\"/browse\")" "src/app/(owner)/equipment/new/page.tsx" | grep -qx 0 && echo PASS || echo FAIL</automated>
  </verify>
  <done>The one-line redirect target is changed; no other behavior in the form changes.</done>
</task>

<task type="auto">
  <name>Task 2: Render equipment thumbnails on owner dashboard and convert CTA to tokens (BUG-IMAGE-01)</name>
  <files>src/app/(owner)/owner/dashboard/page.tsx</files>
  <read_first>
    src/app/(owner)/owner/dashboard/page.tsx (current "My Equipment" `<li>` block, lines 111-121 — text-only; current empty-state CTA, lines 101-108 — hardcoded bg-slate-950)
    src/app/(farmer)/browse/page.tsx (lines 47-66 — exact next/image + getEquipmentImageUrl pattern already correct in production)
  </read_first>
  <action>
    Add `import Image from "next/image";` to the top imports of `src/app/(owner)/owner/dashboard/page.tsx` (not currently imported in this file).

    Replace the "My Equipment" section's `<li>` (current lines 113-120, the bare `<Link>`-only row) with a `flex items-center gap-3` row per UI-SPEC.md §4: `<li key={item.id} className="flex items-center gap-3 rounded-md border p-2 text-sm">` containing first a thumbnail `<div className="relative h-14 w-14 shrink-0 overflow-hidden rounded-md bg-muted">` that renders `item.equipment_images[0]` via `<Image src={listingService.getEquipmentImageUrl(item.equipment_images[0].storage_path)} alt={item.title} fill className="object-cover" />` when present, else a fallback `<div className="flex h-full items-center justify-center text-xs text-muted-foreground">No photo</div>` — this is the exact same conditional shape already proven correct in browse/page.tsx lines 47-66. Then a `<div className="min-w-0 flex-1">` wrapping the existing `<Link href={\`/equipment/${item.id}/edit\`} className="hover:underline">` with `<span className="truncate">{item.title} — ₹{item.rate}/{item.rate_unit}</span>` inside it (add `truncate` since the row is now visually denser per UI-SPEC).

    Convert the empty-state CTA at current lines 102-108 (`<Link href="/equipment/new" className="inline-flex h-9 items-center justify-center rounded-md bg-slate-950 px-4 text-sm font-medium text-white hover:bg-slate-800">List equipment</Link>`) to use the shadcn `Button` component: import `{ Button }` from `@/components/ui/button` at the top of the file, then wrap as `<Button asChild><Link href="/equipment/new">List equipment</Link></Button>` — same copy ("List equipment"), only the styling mechanism changes from hardcoded slate literals to the `Button` component's default (`bg-primary text-primary-foreground`) variant, per UI-SPEC.md Color section and the cross-cutting "Tokens-only color rule" in PATTERNS.md.

    Apply the MOBILE-02 row-wrap fix to the "All Bookings" `<li>` (current line 137): change `className="flex items-center justify-between rounded-md border p-2 text-sm"` to `className="flex flex-col gap-2 rounded-md border p-2 text-sm sm:flex-row sm:items-center sm:justify-between"`. Also add `flex-wrap` to the inner badge+buttons `<div className="flex items-center gap-2">` (current line 147) so it becomes `className="flex flex-wrap items-center gap-2"`, per UI-SPEC.md §2.
  </action>
  <acceptance_criteria>
    <source_assertion>src/app/(owner)/owner/dashboard/page.tsx imports next/image and calls listingService.getEquipmentImageUrl inside the My Equipment list</source_assertion>
    <source_assertion>src/app/(owner)/owner/dashboard/page.tsx does NOT contain the string "bg-slate-950"</source_assertion>
    <source_assertion>src/app/(owner)/owner/dashboard/page.tsx's All Bookings <li> className contains "sm:flex-row sm:items-center sm:justify-between"</source_assertion>
    <test_command>grep -c "bg-slate-950" "src/app/(owner)/owner/dashboard/page.tsx" | grep -qx 0 && echo PASS || echo FAIL</test_command>
  </acceptance_criteria>
  <verify>
    <automated>grep -c "bg-slate-950" "src/app/(owner)/owner/dashboard/page.tsx" | grep -qx 0 && echo PASS || echo FAIL</automated>
  </verify>
  <done>Owner dashboard's My Equipment list shows photo thumbnails (or No photo fallback), the empty-state CTA uses Button/tokens, and All Bookings rows wrap on narrow viewports.</done>
</task>

<task type="auto">
  <name>Task 3: Apply MOBILE-02 row-wrap and token CTA fix to farmer dashboard</name>
  <files>src/app/(farmer)/farmer/dashboard/page.tsx</files>
  <read_first>
    src/app/(farmer)/farmer/dashboard/page.tsx (current "My Bookings" `<li>`, line 126; empty-state CTA, lines 113-119)
  </read_first>
  <action>
    In `src/app/(farmer)/farmer/dashboard/page.tsx`, apply the identical MOBILE-02 fix as Task 2 applied to the owner dashboard's All Bookings list: change the "My Bookings" `<li>` (current line 126) className from `"flex items-center justify-between rounded-md border p-2 text-sm"` to `"flex flex-col gap-2 rounded-md border p-2 text-sm sm:flex-row sm:items-center sm:justify-between"`, and add `flex-wrap` to the inner badge+button `<div className="flex items-center gap-2">` (current line 135) so it becomes `className="flex flex-wrap items-center gap-2"`.

    Convert the empty-state CTA at current lines 113-119 (`<Link href="/browse" className="inline-flex h-9 items-center justify-center rounded-md bg-slate-950 px-4 text-sm font-medium text-white hover:bg-slate-800">Browse equipment</Link>`) to use the shadcn `Button` component exactly as Task 2 did: import `{ Button }` from `@/components/ui/button`, then `<Button asChild><Link href="/browse">Browse equipment</Link></Button>` — same copy ("Browse equipment"), no behavior change.
  </action>
  <acceptance_criteria>
    <source_assertion>src/app/(farmer)/farmer/dashboard/page.tsx's My Bookings <li> className contains "sm:flex-row sm:items-center sm:justify-between"</source_assertion>
    <source_assertion>src/app/(farmer)/farmer/dashboard/page.tsx does NOT contain the string "bg-slate-950"</source_assertion>
    <test_command>grep -c "bg-slate-950" "src/app/(farmer)/farmer/dashboard/page.tsx" | grep -qx 0 && echo PASS || echo FAIL</test_command>
  </acceptance_criteria>
  <verify>
    <automated>grep -c "bg-slate-950" "src/app/(farmer)/farmer/dashboard/page.tsx" | grep -qx 0 && echo PASS || echo FAIL</automated>
  </verify>
  <done>Farmer dashboard's My Bookings rows wrap on narrow viewports and the empty-state CTA uses Button/tokens, with no copy change.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None new | This plan is pure UI/markup and a client-side redirect target fix — no new data enters the system, no new authorization surface is introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.1-04 | Tampering | None applicable | accept | No new input surface; redirect target and image-render fixes operate only on already-validated, already-fetched server data (getEquipmentByOwner already RLS-scoped to owner_id) |
</threat_model>

<verification>
- `npx vitest run` (full suite stays green — this plan touches no service/validation logic covered by existing tests)
- Manual: create equipment A then B as an owner, confirm redirect to /owner/dashboard and both listings visible with thumbnails (BUG-LISTING-01 + BUG-IMAGE-01 combined check)
- Manual: resize to 360px on both dashboards, confirm booking rows stack instead of cramping (MOBILE-02)
- `grep -rn "bg-slate-950" src/app/(owner)/owner/dashboard/page.tsx src/app/(farmer)/farmer/dashboard/page.tsx` returns zero matches
</verification>

<success_criteria>
- Owner can create multiple listings without dead-ending on /browse
- Owner dashboard shows equipment photos
- Both dashboards' booking rows are responsive at 360-414px widths
- No hardcoded slate-950 literals remain in either modified dashboard file
</success_criteria>

<output>
Create `.planning/phases/02.1-mobile-responsiveness-persistent-agrimate-ai-chat-multi-list/02.1-02-SUMMARY.md` when done
</output>
