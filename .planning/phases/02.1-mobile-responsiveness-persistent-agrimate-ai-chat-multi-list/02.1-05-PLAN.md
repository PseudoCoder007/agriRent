---
phase: 02.1-mobile-responsiveness-persistent-agrimate-ai-chat-multi-list
plan: 05
type: execute
wave: 2
depends_on: ["02.1-01"]
files_modified:
  - src/components/chat/chat-widget.tsx
  - src/app/(farmer)/farmer/chat/page.tsx
  - src/app/(owner)/owner/chat/page.tsx
  - src/lib/services/ai.service.ts
autonomous: true
requirements: [CHAT-PERSIST-01, CHAT-BRAND-01]

must_haves:
  truths:
    - "A user's chat conversation survives a full page reload and survives navigating away and back to the chat page"
    - "Every chat message the user sends and every assistant reply is persisted exactly once (not on every stream chunk, not before the stream starts)"
    - "Every UI location that shows the chat assistant's name displays 'AgriMate AI' with tagline 'Your Smart Farming Assistant', not 'AgriRent Assistant'"
    - "The chat page nav link label stays 'Chat' (unchanged) — the rebrand is a page-content treatment, not a nav-item rename"
  artifacts:
    - path: "src/components/chat/chat-widget.tsx"
      provides: "ChatWidget accepting initialMessages prop, persisting each turn via saveChatMessageAction, AgriMate AI branding"
      contains: "AgriMate AI"
    - path: "src/app/(farmer)/farmer/chat/page.tsx"
      provides: "async Server Component fetching chat history via getChatHistoryAction/chatService before rendering ChatWidget"
      contains: "initialMessages"
    - path: "src/app/(owner)/owner/chat/page.tsx"
      provides: "async Server Component fetching chat history via getChatHistoryAction/chatService before rendering ChatWidget"
      contains: "initialMessages"
  key_links:
    - from: "src/app/(farmer)/farmer/chat/page.tsx"
      to: "src/lib/services/chat.service.ts"
      via: "chatService.getChatHistory(userData.user.id) Server Component fetch"
      pattern: "chatService\\.getChatHistory"
    - from: "src/components/chat/chat-widget.tsx"
      to: "src/app/actions/chat.actions.ts"
      via: "saveChatMessageAction call after each turn completes"
      pattern: "saveChatMessageAction"
---

<objective>
Wire the chat backend built in Plan 01 into the existing `ChatWidget`: load prior history on page mount via a Server Component fetch, persist each user message and each completed assistant reply exactly once, and apply the "AgriMate AI" rebrand across the widget's UI strings and the AI's own system prompt.

Purpose: CHAT-PERSIST-01 (history must survive reload/navigation) and CHAT-BRAND-01 (consistent "AgriMate AI" / "Your Smart Farming Assistant" branding). This plan is sequenced after Plan 01 because it imports `getChatHistoryAction`/`saveChatMessageAction`, which do not exist until Plan 01 ships.
Output: `/farmer/chat` and `/owner/chat` show prior conversation history on load, persist new turns, and display the rebranded assistant name/tagline/copy.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02.1-mobile-responsiveness-persistent-agrimate-ai-chat-multi-list/02.1-RESEARCH.md
@.planning/phases/02.1-mobile-responsiveness-persistent-agrimate-ai-chat-multi-list/02.1-UI-SPEC.md
@.planning/phases/02.1-mobile-responsiveness-persistent-agrimate-ai-chat-multi-list/02.1-01-SUMMARY.md
@src/components/chat/chat-widget.tsx
@src/app/(farmer)/browse/page.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add history loading + persistence to ChatWidget, apply rebrand strings</name>
  <files>src/components/chat/chat-widget.tsx</files>
  <read_first>
    src/components/chat/chat-widget.tsx (full current file — 145 lines, streaming loop at lines 65-79, CardTitle at line 96, empty-state body at line 102)
    src/app/actions/chat.actions.ts (from Plan 01 — saveChatMessageAction(role, content) signature)
  </read_first>
  <behavior>
    - ChatWidget({ initialMessages }): when initialMessages is a non-empty array, the rendered message list shows those messages immediately on mount (no flash of empty state).
    - handleSend: after setMessages(nextMessages) on line 41 (the user's own message being added to local state), await saveChatMessageAction("user", content) before proceeding to the fetch call — per RESEARCH.md Pitfall 6, save before the next async step, not fire-and-forget.
    - handleSend: after the streaming for-loop (lines 65-79) exits via `break` (i.e. `done === true`), await saveChatMessageAction("assistant", accumulatedContent) using the final accumulated message content — never save mid-stream (partial content) and never save before the stream starts (empty content).
    - handleSend error path (catch block, lines 80-81): unchanged — no persistence call on the error path, since no complete assistant message exists to save.
  </behavior>
  <action>
    Modify `src/components/chat/chat-widget.tsx`: change the function signature from `export function ChatWidget()` to `export function ChatWidget({ initialMessages = [] }: { initialMessages?: Message[] })`, and change `const [messages, setMessages] = useState<Message[]>([]);` to `const [messages, setMessages] = useState<Message[]>(initialMessages);`. Add `import { saveChatMessageAction } from "@/app/actions/chat.actions";` to the top imports.

    Inside `handleSend`, immediately after `setMessages(nextMessages);` on line 41, add `await saveChatMessageAction("user", content);` (the function is already `async`, so this is a normal await in sequence before the `setInput("")`/`setIsPending(true)` lines that follow — keep those after the save call or reorder minimally so the save happens before the fetch to `/api/chat`, per RESEARCH.md Pattern 4's "should still be awaited before the next turn" guidance).

    After the `for (;;) { ... if (done) break; ... }` loop (currently ending at line 79) exits, before the `catch`/`finally` block structure resolves (i.e., right after the loop, still inside the `try` block), capture the final accumulated assistant content from the last entry of `messages` state at that point — since `setMessages` updates are async, track the accumulated string in a local `let accumulatedContent = ""` variable updated alongside each `setMessages` call inside the loop (add `accumulatedContent += token;` right after `const token = decoder.decode(...)` on line 69, parallel to the existing `setMessages` updater), then call `await saveChatMessageAction("assistant", accumulatedContent);` immediately after the loop's `break`/exit, using that local variable rather than reading back from React state.

    Apply the rebrand: change `<CardTitle>AgriRent Assistant</CardTitle>` (line 96) to `<CardTitle>🥇 AgriMate AI</CardTitle>` followed immediately by `<p className="text-xs font-normal text-muted-foreground">Your Smart Farming Assistant</p>` (placed inside or directly below `CardHeader`, per UI-SPEC.md §3). Change the empty-state body text on line 102 from `"Ask a question about how booking and rental works on AgriRent."` to `"Get instant answers about booking, pricing, or how rentals work on AgriRent."` Leave the `ERROR_MESSAGE` constant and the empty-state heading area otherwise unchanged — UI-SPEC.md keeps the error copy as-is.
  </action>
  <verify>
    <automated>npx vitest run 2>&1 | tail -15</automated>
  </verify>
  <done>ChatWidget accepts and renders initialMessages, persists exactly one row per completed user/assistant turn via saveChatMessageAction, and displays "🥇 AgriMate AI" + tagline + updated empty-state copy.</done>
</task>

<task type="auto">
  <name>Task 2: Fetch chat history server-side in both chat page routes</name>
  <files>src/app/(farmer)/farmer/chat/page.tsx, src/app/(owner)/owner/chat/page.tsx</files>
  <read_first>
    src/app/(farmer)/farmer/chat/page.tsx (current 13-line file — synchronous component, no data fetch)
    src/app/(owner)/owner/chat/page.tsx (identical structure)
    src/app/(farmer)/browse/page.tsx (async Server Component fetch-then-render-client-child pattern to mirror)
  </read_first>
  <action>
    Convert `src/app/(farmer)/farmer/chat/page.tsx` to an async Server Component: import `{ createClient }` from `"@/lib/supabase/server"` and `* as chatService from "@/lib/services/chat.service"`. Change `export default function FarmerChatPage()` to `export default async function FarmerChatPage()`. Inside, call `const supabase = await createClient();`, `const { data: userData } = await supabase.auth.getUser();`, then `const history = userData.user ? (await chatService.getChatHistory(userData.user.id)).data ?? [] : [];`. Pass `<ChatWidget initialMessages={history} />` instead of the current bare `<ChatWidget />`. Keep the existing `<div className="p-4">` wrapper and the file's existing doc comment.

    Apply the identical conversion to `src/app/(owner)/owner/chat/page.tsx` (same imports, same logic, `OwnerChatPage` function name unchanged).
  </action>
  <acceptance_criteria>
    <source_assertion>src/app/(farmer)/farmer/chat/page.tsx calls chatService.getChatHistory and passes initialMessages to ChatWidget</source_assertion>
    <source_assertion>src/app/(owner)/owner/chat/page.tsx calls chatService.getChatHistory and passes initialMessages to ChatWidget</source_assertion>
    <behavior_assertion>Sending a message, then reloading /farmer/chat (or /owner/chat), shows the prior conversation instead of an empty chat (manual browser check per VALIDATION.md — CHAT-PERSIST-01)</behavior_assertion>
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <done>Both chat page routes are async Server Components that fetch and pass prior history into ChatWidget.</done>
</task>

<task type="auto">
  <name>Task 3: Update AI system prompt self-reference (optional rebrand consistency)</name>
  <files>src/lib/services/ai.service.ts</files>
  <read_first>
    src/lib/services/ai.service.ts (buildSystemPrompt() function, line 84: "You are the AgriRent assistant for a farm equipment rental marketplace.")
  </read_first>
  <action>
    In `src/lib/services/ai.service.ts`'s `buildSystemPrompt()` function, change the opening sentence from `"You are the AgriRent assistant for a farm equipment rental marketplace. "` to `"You are AgriMate AI, the assistant for the AgriRent farm equipment rental marketplace. "` — per UI-SPEC.md §3 "Decision: do it" — change ONLY this self-referential string. Do not alter any instruction/guardrail logic in the rest of the system prompt (the "Rules:" list, the marketplace data interpolation, or the "Do NOT create, modify, approve, or cancel bookings or listings" line must remain byte-for-byte identical).
  </action>
  <acceptance_criteria>
    <source_assertion>src/lib/services/ai.service.ts's buildSystemPrompt contains "AgriMate AI" and does NOT contain the literal string "You are the AgriRent assistant"</source_assertion>
    <test_command>npx vitest run src/lib/services/ai.service.test.ts</test_command>
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/lib/services/ai.service.test.ts</automated>
  </verify>
  <done>The system prompt's self-introduction says AgriMate AI; all guardrail/rules text is unchanged; existing ai.service tests still pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> saveChatMessageAction | User-typed content persisted via the Plan 01 Server Action, which already validates via Zod and scopes by server-verified session |
| NVIDIA NIM stream -> persisted assistant row | Accumulated assistant content is only ever read from the local `accumulatedContent` variable built from the same stream already rendered to the user — no additional trust boundary beyond what Plan 1 (chatbot phase) already established |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.1-07 | Tampering | Chat widget persistence timing | mitigate | Assistant message saved exactly once, after stream completion, from a locally-accumulated string — prevents partial/duplicate-row writes (RESEARCH.md Pitfall 6) |
| T-02.1-08 | Information Disclosure | Server Component chat history fetch | mitigate | getChatHistory always scoped by the server-verified userData.user.id from supabase.auth.getUser(), never a client-supplied ID; RLS (Plan 01) is the defense-in-depth backstop |
</threat_model>

<verification>
- `npx vitest run` full suite green
- Manual: send 2-3 messages, reload /farmer/chat, history is present (CHAT-PERSIST-01)
- Manual: navigate away from /owner/chat and back, history is present
- `grep -rn "AgriRent Assistant" src/` returns zero matches (CHAT-BRAND-01)
</verification>

<success_criteria>
- Chat history persists across reload and navigation for both roles
- Each turn produces exactly one row per message (no duplicates, no partial-content rows)
- "AgriMate AI" / "Your Smart Farming Assistant" branding appears everywhere the assistant's name previously showed "AgriRent Assistant"
- Nav link label remains "Chat" (unchanged)
</success_criteria>

<output>
Create `.planning/phases/02.1-mobile-responsiveness-persistent-agrimate-ai-chat-multi-list/02.1-05-SUMMARY.md` when done
</output>
