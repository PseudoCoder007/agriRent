---
phase: 02.1-mobile-responsiveness-persistent-agrimate-ai-chat-multi-list
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/layout.tsx
  - src/components/theme/theme-provider.tsx
  - src/components/theme/theme-toggle.tsx
  - src/app/(farmer)/layout.tsx
  - src/app/(owner)/layout.tsx
autonomous: true
requirements: [THEME-01]

must_haves:
  truths:
    - "A logged-in user (farmer or owner) can click a single toggle button to switch between light and dark mode"
    - "The chosen theme persists across a full page reload (not just client-side navigation)"
    - "The toggle visually updates immediately because globals.css's existing .dark variable block activates correctly"
  artifacts:
    - path: "src/components/theme/theme-provider.tsx"
      provides: "client-side ThemeProvider wrapper around next-themes' NextThemesProvider"
      exports: ["ThemeProvider"]
    - path: "src/components/theme/theme-toggle.tsx"
      provides: "Sun/Moon icon button calling useTheme().setTheme"
      exports: ["ThemeToggle"]
    - path: "src/app/layout.tsx"
      provides: "root layout wrapping {children} in ThemeProvider with attribute=\"class\", html suppressHydrationWarning"
      contains: "attribute=\"class\""
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/components/theme/theme-provider.tsx"
      via: "ThemeProvider wraps {children}"
      pattern: "<ThemeProvider"
    - from: "src/app/(farmer)/layout.tsx"
      to: "src/components/theme/theme-toggle.tsx"
      via: "<ThemeToggle /> rendered in header right-hand control cluster"
      pattern: "<ThemeToggle"
    - from: "src/app/(owner)/layout.tsx"
      to: "src/components/theme/theme-toggle.tsx"
      via: "<ThemeToggle /> rendered in header right-hand control cluster"
      pattern: "<ThemeToggle"
---

<objective>
Wire the already-installed-but-unwired `next-themes` dependency into a working light/dark toggle: a root `ThemeProvider`, a binary Sun/Moon `ThemeToggle` button, and placement of that toggle in both the farmer and owner header control clusters (before `NotificationBell`).

Purpose: THEME-01. RESEARCH.md confirms `next-themes@0.4.6` is installed and `sonner.tsx` already calls `useTheme()` successfully, but no `ThemeProvider` exists anywhere — this is "finish wiring existing infrastructure," not new architecture. `globals.css` already has a complete `.dark` variable block keyed off a `.dark` class selector (`@custom-variant dark (&:is(.dark *));`), so `attribute="class"` on the provider is mandatory (RESEARCH.md Pitfall 1).
Output: Working theme toggle visible in both authenticated layouts, defaulting to `system`, persisting via next-themes' own localStorage mechanism across reloads.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02.1-mobile-responsiveness-persistent-agrimate-ai-chat-multi-list/02.1-RESEARCH.md
@.planning/phases/02.1-mobile-responsiveness-persistent-agrimate-ai-chat-multi-list/02.1-UI-SPEC.md
@src/app/layout.tsx
@src/components/ui/sonner.tsx
@src/app/(farmer)/layout.tsx
@src/app/(owner)/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ThemeProvider and ThemeToggle, wire root layout</name>
  <files>src/components/theme/theme-provider.tsx, src/components/theme/theme-toggle.tsx, src/app/layout.tsx</files>
  <read_first>
    src/app/layout.tsx (current 23-line file — only imports Toaster, no provider wrapping)
    src/components/ui/sonner.tsx (proof useTheme() already works once a provider exists — lines 1-9)
  </read_first>
  <action>
    Create `src/components/theme/theme-provider.tsx` with `"use client";` directive, importing `{ ThemeProvider as NextThemesProvider }` from `"next-themes"` and `type { ComponentProps }` from `"react"`. Export `function ThemeProvider({ children, ...props }: ComponentProps<typeof NextThemesProvider>) { return <NextThemesProvider {...props}>{children}</NextThemesProvider>; }` — this is the exact Shadcn-documented wrapper pattern, no deviation.

    Create `src/components/theme/theme-toggle.tsx` with `"use client";` directive, importing `{ useTheme }` from `"next-themes"`, `{ Moon, Sun }` from `"lucide-react"`, and `{ Button }` from `"@/components/ui/button"`. Export `function ThemeToggle()` rendering a `<Button variant="ghost" size="icon" onClick={() => setTheme(theme === "dark" ? "light" : "dark")} aria-label="Toggle theme" className="h-11 w-11 sm:h-9 sm:w-9">` containing `<Sun className="hidden h-4 w-4 dark:block" />` and `<Moon className="block h-4 w-4 dark:hidden" />` — the `h-11 w-11 sm:h-9 sm:w-9` sizing is the UI-SPEC.md 44px mobile touch-target floor collapsing to the standard 36px `size="icon"` at `sm:` and above.

    Modify `src/app/layout.tsx`: add `import { ThemeProvider } from "@/components/theme/theme-provider";`. Add `suppressHydrationWarning` to the `<html lang="en">` tag (becomes `<html lang="en" suppressHydrationWarning>` — RESEARCH.md Pitfall 2, this is the ONLY place it goes, do not add it to `<body>`). Wrap the existing `{children}` and `<Toaster />` inside `<ThemeProvider attribute="class" defaultTheme="system" enableSystem disableTransitionOnChange>...</ThemeProvider>`. `attribute="class"` is mandatory per RESEARCH.md Pitfall 1 (globals.css's `@custom-variant dark` only reacts to a `.dark` class, not `data-theme`).
  </action>
  <acceptance_criteria>
    <source_assertion>src/app/layout.tsx contains attribute="class" and html lang="en" suppressHydrationWarning</source_assertion>
    <source_assertion>src/components/theme/theme-toggle.tsx imports useTheme from next-themes and renders both a Sun and Moon icon from lucide-react</source_assertion>
    <test_command>npx tsc --noEmit 2>&1 | grep -i "theme" || echo "no theme-related type errors"</test_command>
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -i "theme" || echo "no theme-related type errors"</automated>
  </verify>
  <done>ThemeProvider wraps the app root with class-strategy theming; ThemeToggle component exists and compiles with no type errors.</done>
</task>

<task type="auto">
  <name>Task 2: Place ThemeToggle in farmer and owner header control clusters</name>
  <files>src/app/(farmer)/layout.tsx, src/app/(owner)/layout.tsx</files>
  <read_first>
    src/app/(farmer)/layout.tsx (current right-hand control cluster, line 60: `<div className="flex items-center gap-2">` containing NotificationBell then LogoutButton)
    src/app/(owner)/layout.tsx (identical structure, line 54)
  </read_first>
  <action>
    In both `src/app/(farmer)/layout.tsx` and `src/app/(owner)/layout.tsx`, add `import { ThemeToggle } from "@/components/theme/theme-toggle";` to the imports. Inside the right-hand `<div className="flex items-center gap-2">` block (farmer current line 60-63, owner current line 54-57), insert `<ThemeToggle />` as the FIRST child, before `<NotificationBell userId={userData.user.id} />`, which stays before `<LogoutButton />` — final order is `ThemeToggle, NotificationBell, LogoutButton`, per UI-SPEC.md §6 and PATTERNS.md "Header right-hand control cluster order". Do not touch the auth/role-check logic (lines 19-38 in both files) — unrelated to this phase, defense-in-depth pattern must remain untouched.
  </action>
  <acceptance_criteria>
    <source_assertion>src/app/(farmer)/layout.tsx renders ThemeToggle immediately before NotificationBell inside the header's right-hand div</source_assertion>
    <source_assertion>src/app/(owner)/layout.tsx renders ThemeToggle immediately before NotificationBell inside the header's right-hand div</source_assertion>
    <behavior_assertion>Clicking the toggle in either layout switches html class between "" and "dark" and persists after a full page reload (manual browser check per VALIDATION.md)</behavior_assertion>
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <done>Both authenticated layouts render the theme toggle in the documented position; theme switching and persistence work end to end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None new | Client-side UI preference (theme) stored in localStorage by next-themes itself; no server data, no auth surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.1-05 | None applicable | ThemeProvider/ThemeToggle | accept | Purely client-side cosmetic preference; no sensitive data, no authorization decision ever keys off theme state |
</threat_model>

<verification>
- `npx tsc --noEmit` passes with no new errors
- Manual: toggle theme in farmer layout, reload page, confirm `<html class="dark">` (or absence) persists
- Manual: toggle theme in owner layout, reload page, confirm same persistence
- Manual: confirm sonner toasts (already calling useTheme()) visually match the selected theme once ThemeProvider is wired
</verification>

<success_criteria>
- ThemeProvider wraps the entire app with attribute="class"
- ThemeToggle is visible and functional in both farmer and owner headers
- Theme choice survives a full browser reload
</success_criteria>

<output>
Create `.planning/phases/02.1-mobile-responsiveness-persistent-agrimate-ai-chat-multi-list/02.1-03-SUMMARY.md` when done
</output>
