---
phase: 02.1-mobile-responsiveness-persistent-agrimate-ai-chat-multi-list
plan: 04
type: execute
wave: 2
depends_on: ["02.1-03"]
files_modified:
  - src/components/ui/sheet.tsx
  - src/app/(farmer)/layout.tsx
  - src/app/(owner)/layout.tsx
autonomous: true
requirements: [MOBILE-01]

must_haves:
  truths:
    - "On viewports narrower than 640px, the farmer and owner header nav links no longer overflow/clip — they collapse behind a hamburger trigger"
    - "Opening the hamburger trigger reveals all nav links (including the farmer-only Favorites link) plus ThemeToggle, NotificationBell, and LogoutButton in a slide-in drawer"
    - "At 640px and above, the existing horizontal nav and inline header controls render exactly as before (no regression to desktop layout)"
  artifacts:
    - path: "src/components/ui/sheet.tsx"
      provides: "shadcn Sheet primitive (official registry, Radix Dialog-based drawer)"
      min_lines: 30
    - path: "src/app/(farmer)/layout.tsx"
      provides: "hidden sm:flex horizontal nav + sm:hidden hamburger trigger opening a Sheet with all nav links + controls"
      contains: "SheetContent"
    - path: "src/app/(owner)/layout.tsx"
      provides: "hidden sm:flex horizontal nav + sm:hidden hamburger trigger opening a Sheet with all nav links + controls"
      contains: "SheetContent"
  key_links:
    - from: "src/app/(farmer)/layout.tsx"
      to: "src/components/ui/sheet.tsx"
      via: "Sheet/SheetTrigger/SheetContent import"
      pattern: "from \"@/components/ui/sheet\""
    - from: "src/app/(owner)/layout.tsx"
      to: "src/components/ui/sheet.tsx"
      via: "Sheet/SheetTrigger/SheetContent import"
      pattern: "from \"@/components/ui/sheet\""
---

<objective>
Install the shadcn `Sheet` primitive and use it to collapse the farmer/owner header nav into a hamburger-triggered slide-in drawer below the `sm:` (640px) breakpoint, while leaving the existing horizontal nav and inline header controls untouched at `sm:` and above.

Purpose: MOBILE-01. RESEARCH.md identifies the flat `<nav className="flex gap-3">` row (3-4 links + bell + logout, soon also + theme toggle from Plan 03) as the concrete overflow risk at narrow widths — no fixed-pixel widths exist elsewhere in the codebase, this is the one real gap. UI-SPEC.md §1 mandates the shadcn `Sheet` pattern specifically (not a hand-rolled overlay) since it is not yet installed.
Output: Both header layouts collapse correctly at narrow widths with zero loss of nav links or header controls, and zero change to desktop-width behavior.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02.1-mobile-responsiveness-persistent-agrimate-ai-chat-multi-list/02.1-RESEARCH.md
@.planning/phases/02.1-mobile-responsiveness-persistent-agrimate-ai-chat-multi-list/02.1-UI-SPEC.md
@.planning/phases/02.1-mobile-responsiveness-persistent-agrimate-ai-chat-multi-list/02.1-03-SUMMARY.md
@src/app/(farmer)/layout.tsx
@src/app/(owner)/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install shadcn Sheet component</name>
  <files>src/components/ui/sheet.tsx</files>
  <read_first>
    .planning/phases/02.1-mobile-responsiveness-persistent-agrimate-ai-chat-multi-list/02.1-UI-SPEC.md (Registry Safety section — confirms components.json has empty registries, Sheet is first-party shadcn official, no vetting gate needed)
  </read_first>
  <action>
    Run `npx shadcn@latest add sheet` from the repo root to install the official shadcn `Sheet` component into `src/components/ui/sheet.tsx` (Radix Dialog-based drawer primitive). This is a first-party shadcn registry component — no Package Legitimacy Audit checkpoint needed per UI-SPEC.md Registry Safety ("not required — first-party shadcn registry only"). Do not hand-roll an overlay/portal; do not modify the generated file's internals beyond what the CLI produces.
  </action>
  <acceptance_criteria>
    <source_assertion>src/components/ui/sheet.tsx exists and exports Sheet, SheetTrigger, SheetContent (and related sub-components)</source_assertion>
    <test_command>npx tsc --noEmit 2>&1 | grep -i "sheet" || echo "no sheet-related type errors"</test_command>
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -i "sheet" || echo "no sheet-related type errors"</automated>
  </verify>
  <done>shadcn Sheet component is installed and type-checks cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Collapse owner header nav into Sheet drawer below sm:</name>
  <files>src/app/(owner)/layout.tsx</files>
  <read_first>
    src/app/(owner)/layout.tsx (post-Plan-03 state: header has ThemeToggle, NotificationBell, LogoutButton in right-hand div; horizontal nav at line ~45)
    .planning/phases/02.1-mobile-responsiveness-persistent-agrimate-ai-chat-multi-list/02.1-UI-SPEC.md (Phase-Specific Component Contracts §1 — exact Sheet structure)
  </read_first>
  <action>
    In `src/app/(owner)/layout.tsx`, add imports for `{ Menu }` from `"lucide-react"` and `{ Sheet, SheetContent, SheetTrigger }` from `"@/components/ui/sheet"`. Wrap the existing horizontal `<nav className="flex gap-3 text-sm">` (containing Dashboard, Chat links) in `hidden sm:flex` — it currently always renders, must be hidden below `sm:`.

    Add a new `Sheet` immediately after the existing nav, visible only `<sm:` via a trigger: `<Sheet><SheetTrigger asChild><Button variant="ghost" size="icon" className="h-11 w-11 sm:hidden" aria-label="Open navigation menu"><Menu className="h-5 w-5" /></Button></SheetTrigger><SheetContent side="right" className="w-64">...</SheetContent></Sheet>` (import `Button` from `@/components/ui/button` if not already imported). Inside `SheetContent`, stack the same nav `<Link>` list vertically (`<nav className="flex flex-col gap-1">` containing Dashboard, Chat — identical hrefs/labels to the horizontal nav), followed by a `border-t pt-3` divider, then `ThemeToggle`, `NotificationBell userId={userData.user.id}`, and `LogoutButton` stacked below it — same relative order (Theme, Notifications, Logout) as the desktop header per UI-SPEC.md §6.

    Header row at `<sm:` becomes: brand label (`<span className="text-sm font-medium">AgriRent — Owner</span>`, unchanged) + the new hamburger trigger only — `NotificationBell`/`ThemeToggle`/`LogoutButton` relocate into the Sheet on mobile and must NOT also render inline in the header below `sm:` (wrap the existing right-hand `<div className="flex items-center gap-2">` in `hidden sm:flex` so it only shows inline at `sm:` and above, matching the nav's same breakpoint treatment).
  </action>
  <acceptance_criteria>
    <source_assertion>src/app/(owner)/layout.tsx's horizontal nav has "hidden sm:flex" in its className</source_assertion>
    <source_assertion>src/app/(owner)/layout.tsx contains a SheetContent block rendering ThemeToggle, NotificationBell, and LogoutButton</source_assertion>
    <behavior_assertion>At a 360px viewport width, the owner header shows only the brand label and hamburger trigger, with no nav link overflow (manual devtools check per VALIDATION.md)</behavior_assertion>
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <done>Owner header collapses correctly below sm: with all links/controls reachable via the Sheet; desktop layout at sm: and above is unchanged from Plan 03's state.</done>
</task>

<task type="auto">
  <name>Task 3: Collapse farmer header nav into Sheet drawer below sm: (preserving Favorites link)</name>
  <files>src/app/(farmer)/layout.tsx</files>
  <read_first>
    src/app/(farmer)/layout.tsx (post-Plan-03 state: header has ThemeToggle, NotificationBell, LogoutButton in right-hand div; horizontal nav has FOUR links — Dashboard, Browse, Favorites, Chat — one more than the owner layout)
  </read_first>
  <action>
    Apply the identical structural change as Task 2 to `src/app/(farmer)/layout.tsx`: same imports (`Menu` from lucide-react, `Sheet`/`SheetContent`/`SheetTrigger` from `@/components/ui/sheet`, `Button` if not already imported), same `hidden sm:flex` wrap on the horizontal nav, same `hidden sm:flex` wrap on the right-hand controls div, same hamburger trigger pattern, same `SheetContent side="right" className="w-64"` structure with stacked `<Link>` list then `border-t pt-3` divider then ThemeToggle/NotificationBell/LogoutButton.

    Critical difference from the owner layout: the farmer nav `<Link>` list (both the horizontal version AND the Sheet's stacked version) must include all FOUR links — Dashboard (`/farmer/dashboard`), Browse (`/browse`), Favorites (`/farmer/favorites`), Chat (`/farmer/chat`) — preserve the Favorites link in both places; do not drop it when restructuring (per RESEARCH.md PATTERNS.md explicit warning: "Farmer layout has one extra nav link (Favorites) — preserve it in both the desktop `<nav>` and the Sheet drawer list").
  </action>
  <acceptance_criteria>
    <source_assertion>src/app/(farmer)/layout.tsx's horizontal nav has "hidden sm:flex" in its className</source_assertion>
    <source_assertion>src/app/(farmer)/layout.tsx's SheetContent block contains all four nav links: /farmer/dashboard, /browse, /farmer/favorites, /farmer/chat</source_assertion>
    <behavior_assertion>At a 360px viewport width, the farmer header shows only the brand label and hamburger trigger; opening the drawer reveals all four nav links plus Theme/Notifications/Logout (manual devtools check per VALIDATION.md)</behavior_assertion>
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <done>Farmer header collapses correctly below sm: with all four nav links (including Favorites) and all header controls reachable via the Sheet; desktop layout unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None new | Pure client-side navigation UI restructuring; no new data flow, no new authorization surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.1-06 | None applicable | Sheet drawer nav | accept | Radix Dialog under the hood already handles focus-trap/ESC/ARIA correctly (shadcn-vetted primitive); no custom security-relevant logic introduced |
| T-02.1-SC | Tampering | npx shadcn add sheet | mitigate | First-party shadcn official registry, no third-party registry configured (components.json registries: {}) — no checkpoint needed per UI-SPEC.md Registry Safety, but confirm `src/components/ui/sheet.tsx` content matches the standard shadcn-generated shape before relying on it |
</threat_model>

<verification>
- `npx tsc --noEmit` passes
- Manual: resize to 360px and 414px on `/farmer/dashboard`, `/browse`, `/farmer/favorites`, `/farmer/chat`, `/owner/dashboard`, `/owner/chat` — header never overflows, hamburger drawer opens and contains all links/controls
- Manual: resize back to >=640px — horizontal nav and inline header controls render exactly as they did after Plan 03 (no regression)
</verification>

<success_criteria>
- Both header layouts collapse to a hamburger+drawer pattern below 640px
- All nav links (4 for farmer, 2 for owner) and all three header controls (Theme, Notifications, Logout) are reachable in both the collapsed and expanded states
- No desktop-width regression
</success_criteria>

<output>
Create `.planning/phases/02.1-mobile-responsiveness-persistent-agrimate-ai-chat-multi-list/02.1-04-SUMMARY.md` when done
</output>
