---
phase: 02-equipment-booking-lifecycle-deepening
plan: 03
type: execute
wave: 3
depends_on: ["02-01", "02-02"]
files_modified:
  - src/lib/validations/favorites.schema.ts
  - src/lib/services/favorites.service.ts
  - src/app/actions/favorites.actions.ts
  - src/app/(farmer)/equipment/[id]/FavoriteButton.tsx
  - src/app/(farmer)/equipment/[id]/page.tsx
  - src/app/(farmer)/favorites/page.tsx
  - src/app/(farmer)/layout.tsx
autonomous: true
requirements: [EQUIP-07]

must_haves:
  truths:
    - "A farmer can favorite a piece of equipment from its detail page and find it again later on a dedicated favorites page"
    - "A farmer can unfavorite previously-favorited equipment and it disappears from the favorites page"
  artifacts:
    - path: "src/lib/services/favorites.service.ts"
      provides: "toggleFavorite(farmerId, equipmentId), getFavoritesForFarmer(farmerId), isFavorited(farmerId, equipmentId)"
      contains: "export async function toggleFavorite"
    - path: "src/app/(farmer)/favorites/page.tsx"
      provides: "Farmer-only page listing all of the current user's favorited equipment"
      min_lines: 15
    - path: "src/app/actions/favorites.actions.ts"
      provides: "toggleFavoriteAction Server Action"
      contains: "toggleFavoriteAction"
  key_links:
    - from: "src/app/(farmer)/equipment/[id]/FavoriteButton.tsx"
      to: "src/lib/services/favorites.service.ts"
      via: "toggleFavoriteAction(equipmentId) on click"
      pattern: "toggleFavoriteAction"
---

<objective>
Deliver one vertical slice: a farmer can save equipment from its detail page and retrieve it later on a dedicated `/farmer/favorites` page (EQUIP-07). Depends on the live `favorites` table + RLS from Plan 02-01.

This plan is favorites-only. The booking completed/cancelled state machine that previously shared a plan with this work has been split out into Plan 02-04 (see that plan's objective for why) — this plan touches no file that Plan 02-04 or Plan 02-02 also touches, so it can run in the same wave as 02-04, after 02-02.

Purpose: Completes the "farmers can save favorites" half of Phase 2's goal.
Output: A working favorite-toggle button + favorites page.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-equipment-booking-lifecycle-deepening/02-RESEARCH.md
@.planning/phases/02-equipment-booking-lifecycle-deepening/02-01-SUMMARY.md
@.planning/phases/02-equipment-booking-lifecycle-deepening/02-02-SUMMARY.md

@src/lib/services/notification.service.ts
@src/app/actions/listing.actions.ts
@src/app/(owner)/owner/dashboard/BookingActionButtons.tsx
@src/app/(farmer)/equipment/[id]/page.tsx
@src/app/(farmer)/browse/page.tsx
@src/app/(farmer)/layout.tsx
@src/lib/services/booking.service.test.ts
@types/database.ts
</context>

<artifacts_produced>
## Artifacts this plan produces

- `src/lib/validations/favorites.schema.ts`: `toggleFavoriteSchema = z.object({ equipmentId: z.string().uuid() }).strict()`
- `src/lib/services/favorites.service.ts`: `toggleFavorite(farmerId, equipmentId)`, `getFavoritesForFarmer(farmerId)`, `isFavorited(farmerId, equipmentId)` — all `{success, message, data}`
- `src/app/actions/favorites.actions.ts`: `toggleFavoriteAction(equipmentId: string)`
- `src/app/(farmer)/equipment/[id]/FavoriteButton.tsx`: Client Component, Shadcn `Toggle` + Lucide heart icon
- `src/app/(farmer)/equipment/[id]/page.tsx`: modified to render `FavoriteButton`, passing initial favorited state
- `src/app/(farmer)/favorites/page.tsx`: new Server Component, lists the current farmer's favorited equipment via `getFavoritesForFarmer`
- `src/app/(farmer)/layout.tsx`: modified — adds a nav link to `/farmer/favorites`
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: favorites.service.ts + favorites.schema.ts (new domain, full CRUD)</name>
  <files>
    src/lib/validations/favorites.schema.ts
    src/lib/services/favorites.service.ts
  </files>
  <read_first>
    src/lib/services/notification.service.ts (simplest existing single-table service — closest structural analog: ServiceResult shape, createClient usage, no ownership-chain complexity)
    src/lib/services/booking.service.test.ts (vi.mock pattern for createClient, to replicate in the new favorites.service.test.ts)
    .planning/phases/02-equipment-booking-lifecycle-deepening/02-RESEARCH.md (Code Examples section — toggleFavorite/getFavoritesForFarmer reference implementation already drafted there)
    types/database.ts (confirm the regenerated `favorites` table Row/Insert shape from Plan 02-01 before writing types against it)
  </read_first>
  <behavior>
    - Test 1: `toggleFavorite(farmerId, equipmentId)` returns `{success: true, data: {favorited: true}}` when no existing row is found (inserts a new favorite).
    - Test 2: `toggleFavorite(farmerId, equipmentId)` returns `{success: true, data: {favorited: false}}` when an existing row IS found (deletes it).
    - Test 3: `getFavoritesForFarmer(farmerId)` returns `{success: false, data: null}` when the underlying query errors, with a friendly message (never a raw Postgres error string).
  </behavior>
  <action>
    Create `src/lib/validations/favorites.schema.ts` with `export const toggleFavoriteSchema = z.object({ equipmentId: z.string().uuid() }).strict();` and `export type ToggleFavoriteInput = z.infer<typeof toggleFavoriteSchema>;` — `.strict()` mirrors `createBookingSchema`'s tamper-rejection convention (no extra fields like a forged `farmerId` accepted).

    Create `src/lib/services/favorites.service.ts` with the `ServiceResult<T>` type duplicated locally exactly as every other service file does (no shared import — this is the established per-file convention in this codebase, do not introduce a new shared types file). Implement:
    - `export async function toggleFavorite(farmerId: string, equipmentId: string): Promise<ServiceResult<{ favorited: boolean }>>` — query `favorites` for an existing row matching `farmer_id`/`equipment_id` via `.maybeSingle()`; if found, delete it and return `{success: true, message: "Removed from favorites", data: {favorited: false}}`; if not found, insert `{farmer_id: farmerId, equipment_id: equipmentId}` and return `{success: true, message: "Saved to favorites", data: {favorited: true}}`. Catch and translate any insert error (e.g. a theoretical race on the UNIQUE constraint) to `{success: false, message: "Could not update favorite", data: null}` rather than throwing.
    - `export async function getFavoritesForFarmer(farmerId: string): Promise<ServiceResult<...>>` — select `*, equipments(*, equipment_images(id, storage_path))` filtered by `farmer_id`, ordered by `created_at` descending. Type the return row using `Tables<"favorites">` joined shape, following the `EquipmentWithOwnerAndImages`-style named export pattern from `listing.service.ts`.
    - `export async function isFavorited(farmerId: string, equipmentId: string): Promise<ServiceResult<boolean>>` — single `.maybeSingle()` lookup, returns `{success: true, data: !!row}`. Used by the equipment detail page to set the FavoriteButton's initial pressed state.

    Create `src/lib/services/favorites.service.test.ts` covering the three behaviors above using the same `vi.mock("@/lib/supabase/server", ...)` pattern as `booking.service.test.ts`.
  </action>
  <acceptance_criteria>
    - `npx vitest run src/lib/services/favorites.service.test.ts` passes, exit code 0
    - `toggleFavorite`, `getFavoritesForFarmer`, `isFavorited` all return the exact `{success, message, data}` shape on every code path, including error paths
    - `npx tsc --noEmit -p tsconfig.json` passes with zero errors
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/lib/services/favorites.service.test.ts</automated>
  </verify>
  <done>favorites.service.ts exists with toggleFavorite/getFavoritesForFarmer/isFavorited, all RLS-respecting (createClient, never createAdminClient — favorites is pure self-ownership, no cross-user write needed), all tested.</done>
</task>

<task type="auto">
  <name>Task 2: Favorite button UI + favorites page + wire Server Action</name>
  <files>
    src/app/actions/favorites.actions.ts
    src/app/(farmer)/equipment/[id]/FavoriteButton.tsx
    src/app/(farmer)/equipment/[id]/page.tsx
    src/app/(farmer)/favorites/page.tsx
    src/app/(farmer)/layout.tsx
  </files>
  <read_first>
    src/app/actions/listing.actions.ts (createEquipmentAction — exact session-read + parse + service-call + revalidatePath + return pattern)
    src/app/(owner)/owner/dashboard/BookingActionButtons.tsx (exact Client Component shape: useState pending, sonner toast)
    src/app/(farmer)/equipment/[id]/page.tsx (current detail page layout, to insert FavoriteButton into — read it fresh in case Plan 02-02 changed anything upstream in listing.service.ts that affects this page's data shape)
    src/app/(farmer)/browse/page.tsx (current card grid layout — for visual consistency reference only, not modified by this task)
    src/app/(farmer)/layout.tsx (current nav structure, to add a Favorites link into)
  </read_first>
  <action>
    Create `src/app/actions/favorites.actions.ts` with `export async function toggleFavoriteAction(equipmentId: string): Promise<ActionResult>` — reads the session user via `createClient()` + `auth.getUser()`, rejects with `{success:false}` if absent, validates `equipmentId` with `toggleFavoriteSchema.safeParse({equipmentId})`, calls `favoritesService.toggleFavorite(userData.user.id, equipmentId)`, on success `revalidatePath("/farmer/favorites")` and `revalidatePath(\`/equipment/${equipmentId}\`)`, returns the service result untouched.

    If `src/components/ui/toggle.tsx` does not already exist, install it via `npx shadcn@latest add toggle` before writing the button component.

    Create `src/app/(farmer)/equipment/[id]/FavoriteButton.tsx` as a Client Component: props `{ equipmentId: string; initialFavorited: boolean }`. Use `useState` for optimistic pressed state (matching `BookingActionButtons`'s pending-state pattern), Shadcn `Toggle` wrapping a Lucide `Heart` icon (filled when favorited, outline when not — use the `fill` prop or a conditional `className`). On click, optimistically flip local state, call `toggleFavoriteAction(equipmentId)`, on failure revert the optimistic flip and `toast.error(result.message)`; on success `toast.success(result.message)` (no revert needed, server confirms the optimistic guess).

    Modify `src/app/(farmer)/equipment/[id]/page.tsx`: call `favoritesService.isFavorited(userData.user.id, equipment.id)` alongside the existing `getEquipmentById` call (need to fetch the session user here — read via `createClient()` + `auth.getUser()`), pass the result into `<FavoriteButton equipmentId={equipment.id} initialFavorited={...} />` rendered next to the title/category badge row.

    Create `src/app/(farmer)/favorites/page.tsx` as an async Server Component: read session user, redirect to `/login` if absent, call `favoritesService.getFavoritesForFarmer(userData.user.id)`, render a grid of cards reusing the same `Card`/`CardHeader`/`CardContent`/`CardFooter` structure as `browse/page.tsx` (title, category badge, rate, owner name, linking to `/equipment/[id]`), with an empty-state message "No favorites yet. Browse equipment to save some." when the list is empty.

    Modify `src/app/(farmer)/layout.tsx` to add a nav link to `/farmer/favorites` (e.g. "Favorites") alongside whatever existing nav links it has (browse, dashboard, chat) — match the existing link styling exactly, do not introduce new nav styling.
  </action>
  <acceptance_criteria>
    - `npx tsc --noEmit -p tsconfig.json` passes with zero errors
    - `FavoriteButton.tsx` contains `"use client"` as its first line
    - `toggleFavoriteAction` reads the actor id only from `supabase.auth.getUser()`, never from a function parameter resembling `farmerId`
    - Manual: favorite an item on its detail page, navigate to `/farmer/favorites`, confirm it's listed; click the toggle again, confirm it's removed from the favorites page after a refresh
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>Farmer can favorite/unfavorite from the equipment detail page and see the current list on a dedicated /farmer/favorites page; favorites persist server-side via RLS-respecting writes scoped to the authenticated farmer.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Browser -> toggleFavoriteAction | A forged `equipmentId` for nonexistent/foreign equipment must not crash or insert garbage; UNIQUE constraint + FK constraint are the DB-level backstops |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-02-11 | Information Disclosure / Tampering | `public.favorites` table writes | accept | RLS already enabled with self-ownership policies in Plan 02-01; `toggleFavoriteSchema.strict()` rejects any extra forged field (e.g. a fake `farmer_id`) at the Zod layer before the service is ever called |
</threat_model>

<verification>
- `npx tsc --noEmit -p tsconfig.json` passes with zero errors
- `npx vitest run src/lib/services/favorites.service.test.ts` passes
- Manual: favorite/unfavorite round-trip on /farmer/favorites
</verification>

<success_criteria>
- EQUIP-07: farmer can save/favorite equipment and retrieve it on a dedicated page
</success_criteria>

<output>
Create `.planning/phases/02-equipment-booking-lifecycle-deepening/02-03-SUMMARY.md` when done
</output>
</plan>
