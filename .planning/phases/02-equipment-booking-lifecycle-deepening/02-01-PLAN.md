---
phase: 02-equipment-booking-lifecycle-deepening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0005_phase2_schema.sql
  - types/database.ts
autonomous: true
requirements: [EQUIP-03, EQUIP-07, BOOK-05]

must_haves:
  truths:
    - "The live Supabase project has a public.favorites table with RLS enabled and three self-ownership policies (select/insert/delete)"
    - "public.equipments has a deleted_at column that defaults to null"
    - "An UPDATE on public.bookings that jumps status from 'pending' directly to 'completed' is rejected by a database trigger, independent of any application code"
    - "types/database.ts reflects the new favorites table and equipments.deleted_at column so TypeScript compiles against the real live schema"
  artifacts:
    - path: "supabase/migrations/0005_phase2_schema.sql"
      provides: "favorites table + RLS, equipments.deleted_at column, bookings status-transition BEFORE UPDATE trigger"
      contains: "CREATE TABLE public.favorites"
    - path: "types/database.ts"
      provides: "Regenerated Supabase TypeScript types including favorites and equipments.deleted_at"
      contains: "favorites"
  key_links:
    - from: "supabase/migrations/0005_phase2_schema.sql"
      to: "live Supabase project (bzxrqbrinrgbdxgtoyld, linked)"
      via: "supabase db push"
      pattern: "CREATE TABLE public\\.favorites"
---

<objective>
Add the three pieces of net-new schema Phase 2 needs — a `favorites` table (with RLS), a `deleted_at` soft-delete column on `equipments`, and a `BEFORE UPDATE` trigger enforcing the booking status `VALID_TRANSITIONS` state machine — write them as a single migration file, push that migration to the live (already-linked) Supabase project, and regenerate `types/database.ts` so every later plan in this phase compiles against the real schema instead of stale types.

Purpose: Plans 02-02 and 02-03 both write application code (`favorites.service.ts`, `listing.service.ts` soft-delete reads, `booking.service.ts` completed/cancelled transitions) that hard-depends on this schema existing live, not just in a migration file on disk. This plan is the blocking foundation wave — nothing else in Phase 2 can execute until it completes.
Output: `supabase/migrations/0005_phase2_schema.sql` applied to the live database; `types/database.ts` regenerated and committed.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-equipment-booking-lifecycle-deepening/02-RESEARCH.md

@supabase/migrations/0001_init_schema.sql
@types/database.ts
</context>

<artifacts_produced>
## Artifacts this plan produces

- `supabase/migrations/0005_phase2_schema.sql` — new migration file containing:
  - `CREATE TABLE public.favorites (id, farmer_id, equipment_id, created_at)` with `UNIQUE (farmer_id, equipment_id)`
  - `ALTER TABLE public.favorites ENABLE ROW LEVEL SECURITY`
  - Policies: `"favorites select own"`, `"favorites insert own"`, `"favorites delete own"`
  - `ALTER TABLE public.equipments ADD COLUMN deleted_at timestamptz`
  - Function `public.enforce_booking_status_transition()` (`LANGUAGE plpgsql`, row-level `BEFORE UPDATE` trigger function)
  - Trigger `bookings_status_transition_guard` on `public.bookings`
- Regenerated `types/database.ts` (adds `favorites` table types, `equipments.deleted_at` field)
- Live schema change applied to Supabase project `bzxrqbrinrgbdxgtoyld` (already linked per `supabase projects list`)
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Write the Phase 2 schema migration file</name>
  <files>supabase/migrations/0005_phase2_schema.sql</files>
  <read_first>
    supabase/migrations/0001_init_schema.sql (exact header-comment style, per-table RLS-immediately-after-CREATE convention, SECURITY DEFINER/LANGUAGE plpgsql helper pattern, existing `bookings_no_overlap` EXCLUDE constraint and its `WHERE (status IN ('pending', 'approved'))` clause — do not touch this constraint)
    supabase/migrations/0002_equipment_images_bucket.sql, supabase/migrations/0003_users_public_read.sql, supabase/migrations/0004_users_insert_policy.sql (confirm the highest existing migration number before naming this file — list the supabase/migrations/ directory first; if a migration numbered 0005 already exists by the time this task runs, use the next free integer instead and update this plan's files_modified/artifacts references accordingly in the SUMMARY)
    .planning/phases/02-equipment-booking-lifecycle-deepening/02-RESEARCH.md (Pattern 2, Pattern 4, Code Examples sections — exact SQL already drafted there)
  </read_first>
  <action>
    First, list `supabase/migrations/` to confirm the next free sequential number (this plan assumes `0005`, following the existing `0001`-`0004` files; if the directory has changed since this plan was written, use the actual next integer instead and name the file accordingly).

    Create `supabase/migrations/0005_phase2_schema.sql` (or the actual next free number) with a header comment in the same style as `0001_init_schema.sql` (state which tables/columns are added: favorites table, equipments.deleted_at, bookings status-transition trigger). Write three sections in this exact order:

    1. `CREATE TABLE public.favorites (id uuid PRIMARY KEY DEFAULT gen_random_uuid(), farmer_id uuid NOT NULL REFERENCES public.users(id) ON DELETE CASCADE, equipment_id uuid NOT NULL REFERENCES public.equipments(id) ON DELETE CASCADE, created_at timestamptz NOT NULL DEFAULT now(), UNIQUE (farmer_id, equipment_id))` immediately followed by `ALTER TABLE public.favorites ENABLE ROW LEVEL SECURITY;` in the same statement block (per Pitfall 1 in RESEARCH.md — never separate CREATE TABLE from ENABLE ROW LEVEL SECURITY). Add exactly three policies, each scoped to `farmer_id = (select auth.uid())` with no helper function (pure self-ownership, no cross-table check): `"favorites select own"` (FOR SELECT), `"favorites insert own"` (FOR INSERT, WITH CHECK), `"favorites delete own"` (FOR DELETE, USING).

    2. `ALTER TABLE public.equipments ADD COLUMN deleted_at timestamptz;` — no default, nullable, absent means "not deleted." No new RLS policy needed on `equipments` for this column — existing `equipments select authenticated` / `update own as owner` / `delete own as owner` policies from `0001_init_schema.sql` remain unchanged; the soft-delete filter is applied at the service-layer query level in Plan 02-02, not via RLS.

    3. Create function `public.enforce_booking_status_transition()` returning `trigger`, `LANGUAGE plpgsql` (never `LANGUAGE sql` — see 0001's own comment on why plpgsql is required to avoid inlining), no `SECURITY DEFINER` needed (this is a row trigger, not a cross-table RLS helper). Body: if `OLD.status = NEW.status`, `RETURN NEW` (no-op, not a transition). Allow exactly these two forward transitions: `OLD.status = 'pending' AND NEW.status IN ('approved', 'rejected', 'cancelled')`, and `OLD.status = 'approved' AND NEW.status IN ('completed', 'cancelled')` — `RETURN NEW` for both. Otherwise `RAISE EXCEPTION 'Invalid booking status transition: % -> %', OLD.status, NEW.status;`. Then `CREATE TRIGGER bookings_status_transition_guard BEFORE UPDATE ON public.bookings FOR EACH ROW EXECUTE FUNCTION public.enforce_booking_status_transition();`.

    Do not modify `supabase/migrations/0001_init_schema.sql` or any other existing migration file — it has already been applied; this is purely additive in a new file.
  </action>
  <acceptance_criteria>
    - A new file under `supabase/migrations/` exists and contains the exact string `CREATE TABLE public.favorites`
    - File contains `ENABLE ROW LEVEL SECURITY` on the same logical statement block as the favorites table (within 10 lines of the CREATE TABLE)
    - File contains exactly 3 `CREATE POLICY` statements referencing `favorites`
    - File contains `ADD COLUMN deleted_at timestamptz`
    - File contains `CREATE TRIGGER bookings_status_transition_guard`
    - File does NOT contain any modification to `bookings_no_overlap` or its `WHERE` clause
    - No existing migration file (`0001`-`0004`) was modified
  </acceptance_criteria>
  <verify>
    <automated>grep -rc "CREATE TABLE public.favorites" supabase/migrations/</automated>
  </verify>
  <done>Migration file written, matches the header/RLS-adjacency conventions of 0001_init_schema.sql, contains all three schema additions with no edits to the existing EXCLUDE constraint or any prior migration file.</done>
</task>

<task type="auto" gate="blocking">
  <name>Task 2: [BLOCKING] Push migration to live Supabase and regenerate types</name>
  <files>types/database.ts</files>
  <read_first>
    supabase/migrations/0005_phase2_schema.sql (the file just written in Task 1 — confirm its actual filename if it was renumbered)
    types/database.ts (current generated shape, to compare against after regeneration)
  </read_first>
  <action>
    This task is BLOCKING — no other Phase 2 plan (02-02, 02-03) may execute until this task completes successfully, because their service-layer code references `favorites`, `equipments.deleted_at`, and relies on the live transition trigger existing.

    Run `npx supabase db push` from the project root to apply the new migration to the linked live project (project ref `bzxrqbrinrgbdxgtoyld`, confirmed linked via `npx supabase projects list`). This pushes directly against the remote Postgres instance — it does not require Docker/local Supabase (the project has no local dev stack running, confirmed by `npx supabase status` failing on Docker connection, which is expected and not a blocker for `db push`).

    After the push succeeds, run `npx supabase gen types typescript --linked > types/database.ts` to regenerate the TypeScript types file from the live schema, per CLAUDE.md's Database convention ("Run `supabase gen types typescript --linked > types/database.ts` after every schema change").

    If `npx supabase db push` fails with an authentication/login error, this is the one legitimate checkpoint: surface the exact CLI error to the user and stop — do not attempt to work around it by manually editing `types/database.ts` by hand, since that would create exactly the "tsc passes but live DB was never touched" gap this task exists to prevent.
  </action>
  <acceptance_criteria>
    - `npx supabase db push` exits 0 (or reports "already up to date" only if a prior run already applied it — never silently skipped)
    - `types/database.ts` after regeneration contains a `favorites` key under `public.Tables`
    - `types/database.ts` after regeneration contains `deleted_at` under `public.Tables.equipments.Row`
    - `npx tsc --noEmit -p tsconfig.json` passes with zero errors after the regenerated types file is in place
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>Live Supabase schema has favorites table + RLS, equipments.deleted_at column, and the bookings_status_transition_guard trigger; types/database.ts regenerated and committed; tsc passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Migration file -> live Postgres | The migration is the only path that can alter the production schema; a missing or malformed RLS policy here is invisible to TypeScript and only surfaces as a live data-exposure bug |
| Direct Supabase client calls -> bookings table | Any future code path (including a bug in Plan 02-03) that writes a status update is still bound by the trigger even if the service-layer check is wrong or missing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-02-01 | Information Disclosure / Tampering | `public.favorites` (new table) | mitigate | RLS enabled in the same statement block as CREATE TABLE; three self-ownership policies (`farmer_id = auth.uid()`) added before any application code can write to the table — closes the exact "RLS forgotten on new table" pattern flagged in RESEARCH.md Pitfall 1 |
| T-02-02 | Tampering | `public.bookings` (status column) | mitigate | `bookings_status_transition_guard` BEFORE UPDATE trigger rejects any `OLD.status -> NEW.status` jump not in the explicit allow-list, independent of and in addition to the service-layer guard added in Plan 02-04 — defense-in-depth per RESEARCH.md Pattern 4 |
| T-02-03 | Tampering (supply chain) | `npx supabase` CLI invocation | accept | Supabase CLI is an existing, already-used Phase 1 dependency (not a new package); no new npm install occurs in this plan |
</threat_model>

<verification>
- `npx tsc --noEmit -p tsconfig.json` passes with zero errors after types regeneration
- `npx supabase db push` reports the migration applied (or already applied) with no errors
- Manual: query `select * from public.favorites limit 1;` via Supabase SQL editor or `npx supabase db diff` confirms the table exists live, not just in the migration file on disk
</verification>

<success_criteria>
- New migration file exists, committed, matches the conventions of `0001_init_schema.sql`
- Live Supabase project has `favorites` table (with RLS + 3 policies), `equipments.deleted_at` column, and `bookings_status_transition_guard` trigger
- `types/database.ts` regenerated and reflects all three schema changes
- `npx tsc --noEmit -p tsconfig.json` passes
</success_criteria>

<output>
Create `.planning/phases/02-equipment-booking-lifecycle-deepening/02-01-SUMMARY.md` when done
</output>
